---
phase: 02-balance
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - types/index.ts
  - lib/balance.ts
  - lib/db/queries/mois.ts
  - lib/db/queries/depenses.ts
  - lib/db/queries/ajustements.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, RPT-01, RPT-02]

must_haves:
  truths:
    - "calculerBalance() retourne le bon résultat pour mars 2026 (balance_finale = -518.5)"
    - "getOrCreateCurrentMois() crée un mois si absent et reporte la balance du mois précédent"
    - "getOrCreateCurrentMois() ne crée pas de doublon grâce au unique index"
    - "getDepensesByMois() et getAjustementsByMois() retournent des types Drizzle inférés"
    - "types/index.ts exporte Mois, Depense, Ajustement via InferSelectModel"
  artifacts:
    - path: "types/index.ts"
      provides: "Types Drizzle inférés"
      exports: ["Mois", "Depense", "Ajustement"]
    - path: "lib/balance.ts"
      provides: "Algorithme de balance corrigé"
      exports: ["calculerBalance", "BalanceResult"]
    - path: "lib/db/queries/mois.ts"
      provides: "Requêtes mois + report automatique"
      exports: ["getOrCreateCurrentMois"]
    - path: "lib/db/queries/depenses.ts"
      provides: "Requêtes dépenses"
      exports: ["getDepensesByMois"]
    - path: "lib/db/queries/ajustements.ts"
      provides: "Requêtes ajustements"
      exports: ["getAjustementsByMois"]
  key_links:
    - from: "lib/db/queries/mois.ts"
      to: "lib/balance.ts"
      via: "import { calculerBalance } pour computeBalanceReportee"
      pattern: "calculerBalance"
    - from: "lib/db/queries/mois.ts"
      to: "lib/db/schema.ts"
      via: "import { mois, depenses, ajustements }"
    - from: "lib/balance.ts"
      to: "types/index.ts"
      via: "import type { Depense, Ajustement }"
---

<objective>
Créer la couche logique métier complète : types TypeScript, algorithme de balance, et requêtes DB.

Purpose: Ce plan fournit les contracts et l'implémentation sur lesquels le dashboard (Plan 03) s'appuie. L'algorithme de balance est le coeur fonctionnel de l'app — il doit être correct dès le départ (vérifié contre fixtures_e2e.json).
Output: 5 fichiers — types/index.ts, lib/balance.ts, lib/db/queries/{mois,depenses,ajustements}.ts
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-balance/02-RESEARCH.md
@lib/db/schema.ts
@lib/categories.ts
@docs/fixtures_e2e.json

<interfaces>
<!-- Types Drizzle existants dans lib/db/schema.ts -->
```typescript
// lib/db/schema.ts — tables actuelles
export const mois = sqliteTable('mois', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  annee: integer('annee').notNull(),
  mois: integer('mois').notNull(),
  balance_reportee: real('balance_reportee').notNull().default(0),
  cree_le: integer('cree_le', { mode: 'timestamp' }).notNull().$defaultFn(() => new Date()),
});

export const depenses = sqliteTable('depenses', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  mois_id: integer('mois_id').notNull().references(() => mois.id, { onDelete: 'cascade' }),
  categorie: text('categorie').notNull(),
  sous_categorie: text('sous_categorie').notNull(),
  paye_par: text('paye_par').notNull(),
  montant: real('montant').notNull(),
  label: text('label'),
  date_depense: integer('date_depense', { mode: 'timestamp' }).notNull(),
  cree_le: integer('cree_le', { mode: 'timestamp' }).notNull().$defaultFn(() => new Date()),
});

export const ajustements = sqliteTable('ajustements', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  mois_id: integer('mois_id').notNull().references(() => mois.id, { onDelete: 'cascade' }),
  de: text('de').notNull(),
  vers: text('vers').notNull(),
  montant: real('montant').notNull(),
  label: text('label').notNull(),
  date_ajustement: integer('date_ajustement', { mode: 'timestamp' }).notNull(),
  cree_le: integer('cree_le', { mode: 'timestamp' }).notNull().$defaultFn(() => new Date()),
});
```

<!-- Pattern DB client existant -->
```typescript
// lib/db/index.ts — import pattern
import { db } from '@/lib/db';
// db est une instance Drizzle connectée à Turso
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: types/index.ts et lib/balance.ts</name>
  <files>types/index.ts, lib/balance.ts</files>
  <behavior>
    - calculerBalance([], [], 0) → { total_chris: 0, total_alex: 0, balance_mensuelle: 0, balance_reportee: 0, total_chris_vers_alex: 0, total_alex_vers_chris: 0, balance_finale: 0 }
    - calculerBalance([{paye_par:'chris', montant:100}], [], 0) → balance_mensuelle: -50, balance_finale: -50 (Alex doit à Chris)
    - calculerBalance([{paye_par:'alex', montant:200}], [], 0) → balance_mensuelle: 100, balance_finale: 100 (Chris doit à Alex)
    - Mars 2026: chris=33, alex=146, bm=56.5, br=-290, adj_cv=115(appart), adj_vc=400(virement) → balance_finale = -518.5
    - Formule CORRIGÉE: total_chris_vers_alex = balance_mensuelle + balance_reportee + adj(chris→alex)
    - NE PAS utiliser: total_chris_vers_alex = balance_reportee + adj(chris→alex) — formule erronée dans CLAUDE.md
  </behavior>
  <action>
    **1. Créer types/index.ts**

    ```typescript
    // types/index.ts
    import type { InferSelectModel } from 'drizzle-orm';
    import type { mois, depenses, ajustements } from '@/lib/db/schema';

    export type Mois       = InferSelectModel<typeof mois>;
    export type Depense    = InferSelectModel<typeof depenses>;
    export type Ajustement = InferSelectModel<typeof ajustements>;
    ```

    **2. Créer lib/balance.ts**

    ```typescript
    // lib/balance.ts
    import type { Depense, Ajustement } from '@/types';

    export interface BalanceResult {
      total_chris: number;
      total_alex: number;
      balance_mensuelle: number;         // (total_alex - total_chris) / 2
      balance_reportee: number;          // reporté du mois précédent
      total_chris_vers_alex: number;     // balance_mensuelle + balance_reportee + adj(chris→alex)
      total_alex_vers_chris: number;     // adj(alex→chris)
      balance_finale: number;            // total_cv - total_vc
      // > 0: Chris doit à Alex  |  < 0: Alex doit à Chris  |  = 0: équilibre
    }

    export function calculerBalance(
      depenses: Depense[],
      ajustements: Ajustement[],
      balance_reportee: number,
    ): BalanceResult {
      const total_chris = depenses
        .filter((d) => d.paye_par === 'chris')
        .reduce((sum, d) => sum + d.montant, 0);

      const total_alex = depenses
        .filter((d) => d.paye_par === 'alex')
        .reduce((sum, d) => sum + d.montant, 0);

      // (total_alex - total_chris) / 2 : si Alex a plus dépensé, Chris doit compenser
      const balance_mensuelle = (total_alex - total_chris) / 2;

      // ALGORITHME CORRIGÉ (vérifié contre fixtures_e2e.json mars 2026)
      // NE PAS omettre balance_mensuelle — CLAUDE.md a une erreur de documentation
      const total_chris_vers_alex =
        balance_mensuelle +
        balance_reportee +
        ajustements
          .filter((a) => a.de === 'chris' && a.vers === 'alex')
          .reduce((sum, a) => sum + a.montant, 0);

      const total_alex_vers_chris = ajustements
        .filter((a) => a.de === 'alex' && a.vers === 'chris')
        .reduce((sum, a) => sum + a.montant, 0);

      const balance_finale = total_chris_vers_alex - total_alex_vers_chris;

      return {
        total_chris,
        total_alex,
        balance_mensuelle,
        balance_reportee,
        total_chris_vers_alex,
        total_alex_vers_chris,
        balance_finale,
      };
    }
    ```

    Vérification manuelle mars 2026 (extrait fixture):
    - total_chris=33, total_alex=146 → bm=56.5
    - br=-290, adj_cv=115(appart chris→alex)
    - total_cv = 56.5 + (-290) + 115 = -118.5
    - adj_vc=400(virement alex→chris) → total_vc=400
    - balance_finale = -118.5 - 400 = -518.5 ✓
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "types/index|lib/balance" | head -10 || echo "no TS errors"</automated>
  </verify>
  <done>
    - types/index.ts exporte Mois, Depense, Ajustement
    - lib/balance.ts exporte calculerBalance et BalanceResult
    - Pas d'erreur TypeScript
    - L'algorithme est la version corrigée (avec balance_mensuelle dans total_chris_vers_alex)
  </done>
</task>

<task type="auto">
  <name>Task 2: lib/db/queries/ — mois, depenses, ajustements</name>
  <files>lib/db/queries/mois.ts, lib/db/queries/depenses.ts, lib/db/queries/ajustements.ts</files>
  <action>
    Créer le répertoire lib/db/queries/ et les trois fichiers de requêtes.

    **lib/db/queries/depenses.ts**
    ```typescript
    import { db } from '@/lib/db';
    import { depenses } from '@/lib/db/schema';
    import { eq } from 'drizzle-orm';

    export async function getDepensesByMois(moisId: number) {
      return db.select().from(depenses).where(eq(depenses.mois_id, moisId));
    }
    ```

    **lib/db/queries/ajustements.ts**
    ```typescript
    import { db } from '@/lib/db';
    import { ajustements } from '@/lib/db/schema';
    import { eq } from 'drizzle-orm';

    export async function getAjustementsByMois(moisId: number) {
      return db.select().from(ajustements).where(eq(ajustements.mois_id, moisId));
    }
    ```

    **lib/db/queries/mois.ts**
    ```typescript
    import { db } from '@/lib/db';
    import { mois, depenses, ajustements } from '@/lib/db/schema';
    import { eq, and } from 'drizzle-orm';
    import { calculerBalance } from '@/lib/balance';

    export async function getOrCreateCurrentMois() {
      const now = new Date();
      const annee = now.getFullYear();
      const moisNum = now.getMonth() + 1; // 1-12

      // Chercher le mois courant existant
      const existing = await db
        .select()
        .from(mois)
        .where(and(eq(mois.annee, annee), eq(mois.mois, moisNum)))
        .limit(1);

      if (existing.length > 0) return existing[0];

      // Pas trouvé — calculer la balance_reportee depuis le mois précédent (RPT-01)
      const balance_reportee = await computeBalanceReportee(annee, moisNum);

      // Insérer le nouveau mois (RPT-02: jamais manuel)
      // onConflictDoUpdate avec no-op protège contre les race conditions rares
      const [inserted] = await db
        .insert(mois)
        .values({ annee, mois: moisNum, balance_reportee })
        .onConflictDoUpdate({
          target: [mois.annee, mois.mois],
          set: { balance_reportee: balance_reportee }, // no-op si déjà créé en parallèle
        })
        .returning();

      return inserted;
    }

    async function computeBalanceReportee(annee: number, moisNum: number): Promise<number> {
      // Mois précédent calendaire
      const prevMoisNum = moisNum === 1 ? 12 : moisNum - 1;
      const prevAnnee   = moisNum === 1 ? annee - 1 : annee;

      const prevRows = await db
        .select()
        .from(mois)
        .where(and(eq(mois.annee, prevAnnee), eq(mois.mois, prevMoisNum)))
        .limit(1);

      if (prevRows.length === 0) return 0; // Premier mois — balance_reportee = 0

      const prevMois = prevRows[0];

      const prevDepenses = await db
        .select()
        .from(depenses)
        .where(eq(depenses.mois_id, prevMois.id));

      const prevAjustements = await db
        .select()
        .from(ajustements)
        .where(eq(ajustements.mois_id, prevMois.id));

      const { balance_finale } = calculerBalance(
        prevDepenses,
        prevAjustements,
        prevMois.balance_reportee,
      );

      return balance_finale;
    }
    ```

    Note sur onConflictDoUpdate: Drizzle ORM pour SQLite/Turso supporte `.onConflictDoUpdate()`. Le `target` doit référencer les colonnes (pas l'index nommé). Utiliser `[mois.annee, mois.mois]` comme target. Si l'API Drizzle pour Turso ne supporte pas onConflictDoUpdate, utiliser `.onConflictDoNothing()` à la place.

    Note sur les alias : la variable locale `moisNum` évite la confusion avec le champ `mois.mois` (table nommée mois, colonne nommée mois).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "lib/db/queries" | head -10 || echo "no TS errors in queries"</automated>
  </verify>
  <done>
    - lib/db/queries/mois.ts exporte getOrCreateCurrentMois
    - lib/db/queries/depenses.ts exporte getDepensesByMois
    - lib/db/queries/ajustements.ts exporte getAjustementsByMois
    - Pas d'erreur TypeScript
    - getOrCreateCurrentMois gère le report automatique (RPT-01) et la protection contre les doublons
  </done>
</task>

</tasks>

<verification>
- npx tsc --noEmit — 0 erreurs TypeScript
- npx playwright test tests/balance.spec.ts — toujours 0 failures (stubs)
- Tous les fichiers ont des exports nommés corrects
</verification>

<success_criteria>
- L'algorithme calculerBalance produit balance_finale = -518.5 pour le scénario mars 2026 des fixtures
- getOrCreateCurrentMois crée automatiquement un nouveau mois avec balance_reportee = balance_finale du mois précédent
- Les types Mois/Depense/Ajustement sont inférés de Drizzle (pas d'interfaces manuelles)
- Aucun warning TypeScript strict
</success_criteria>

<output>
After completion, create `.planning/phases/02-balance/02-02-SUMMARY.md`
</output>
