---
phase: 02-balance
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - tests/balance.spec.ts
  - tests/helpers/seed.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, RPT-01]

must_haves:
  truths:
    - "npx playwright test tests/balance.spec.ts passe entièrement (0 failures, 0 skipped)"
    - "DASH-01: le dashboard affiche le bon débiteur et montant après seed"
    - "DASH-02: total_chris, total_alex, balance_mensuelle, balance_reportee sont corrects"
    - "DASH-03: les 4 catégories sont visibles avec leurs montants"
    - "DASH-04: deux chargements successifs produisent les mêmes données fraîches"
    - "RPT-01: un second mois créé a balance_reportee = balance_finale du mois précédent"
  artifacts:
    - path: "tests/balance.spec.ts"
      provides: "Tests E2E complets pour DASH-01 à RPT-01"
      contains: "seedDatabase"
  key_links:
    - from: "tests/balance.spec.ts"
      to: "http://localhost:3000/"
      via: "page.goto('/') après loginAs"
    - from: "tests/balance.spec.ts"
      to: "tests/helpers/seed.ts"
      via: "seedDatabase(SCENARIO_SIMPLE)"
---

<objective>
Remplacer les stubs todo() par des tests E2E complets qui valident le comportement réel du dashboard.

Purpose: C'est la validation finale de la phase 2. Les tests confirment que l'algorithme, les requêtes DB et les composants fonctionnent ensemble de bout en bout — pas seulement individuellement.
Output: tests/balance.spec.ts complet, tests/helpers/seed.ts finalisé si besoin.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-balance/02-RESEARCH.md
@.planning/phases/02-balance/02-VALIDATION.md
@tests/auth.spec.ts
@docs/fixtures_e2e.json
@.planning/phases/02-balance/02-03-SUMMARY.md

<interfaces>
<!-- data-testid disponibles dans le dashboard (créés par Plan 03) -->
```
balance-finale      — montant de la balance finale formaté en EUR
balance-debiteur    — texte "Chris doit X € à Alex" ou "Alex doit X € à Chris"
total-chris         — total dépenses Chris formaté en EUR
total-alex          — total dépenses Alex formaté en EUR
balance-mensuelle   — balance mensuelle avec signe (+ / -)
balance-reportee    — report du mois précédent avec signe (+ / -)
synthese-alimentation    — ligne catégorie alimentation
synthese-habitation      — ligne catégorie habitation
synthese-loisirs         — ligne catégorie loisirs
synthese-vie_quotidienne — ligne catégorie vie quotidienne
```

<!-- Scénario de test recommandé (simple, calcul mental facile) -->
```
SCENARIO_SIMPLE:
  mois:     mars 2026, balance_reportee = 0
  depenses: Chris paie 100€ (alimentation), Alex paie 200€ (alimentation)
  ajust.:   aucun
  attendu:
    total_chris = 100
    total_alex  = 200
    balance_mensuelle = (200-100)/2 = 50
    balance_reportee = 0
    total_chris_vers_alex = 50 + 0 + 0 = 50
    total_alex_vers_chris = 0
    balance_finale = 50 → "Chris doit 50,00 € à Alex"

SCENARIO_REPORT:
  mois A: février 2026, balance_reportee = 0
    depenses: Chris 100€, Alex 0€
    balance_finale = (0-100)/2 = -50 (Alex doit 50 à Chris)
  mois B: mars 2026, auto-créé par getOrCreateCurrentMois
    balance_reportee attendue = -50 (= balance_finale de février)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implémenter tests DASH-01, DASH-02, DASH-03, DASH-04</name>
  <files>tests/balance.spec.ts</files>
  <action>
    Remplacer les stubs todo() par des tests E2E concrets. Utiliser SCENARIO_SIMPLE (calcul prévisible, erreur facile à diagnostiquer).

    Structure du fichier tests/balance.spec.ts :

    ```typescript
    import { test, expect } from '@playwright/test';
    import { loginAs } from './helpers/auth';
    import { seedDatabase } from './helpers/seed';

    // Scénario de base : calcul simple et prévisible
    // Chris paie 100€, Alex paie 200€ en alimentation, mars 2026
    // balance_mensuelle = (200-100)/2 = 50
    // balance_reportee = 0 (injecté en seed)
    // Aucun ajustement
    // balance_finale = 50 > 0 → "Chris doit 50,00 € à Alex"
    const SCENARIO_SIMPLE = {
      mois: [{ annee: 2026, mois: 3, balance_reportee: 0 }],
      depenses: [
        {
          annee: 2026, mois: 3,
          categorie: 'alimentation', sous_categorie: 'marche_pain',
          paye_par: 'chris' as const, montant: 100,
          label: 'courses chris', date_depense: '2026-03-01',
        },
        {
          annee: 2026, mois: 3,
          categorie: 'alimentation', sous_categorie: 'intermarche',
          paye_par: 'alex' as const, montant: 200,
          label: 'intermarche alex', date_depense: '2026-03-02',
        },
      ],
    };

    test.describe('DASH-01: Balance du mois courant visible', () => {
      test('dashboard shows balance_finale and debtor label', async ({ page }) => {
        await seedDatabase(SCENARIO_SIMPLE);
        await loginAs(page, 'chris');
        await page.goto('/');

        // Le montant de la balance finale doit être affiché
        const balanceFinale = page.getByTestId('balance-finale');
        await expect(balanceFinale).toBeVisible();
        await expect(balanceFinale).toContainText('50');

        // Le label débiteur doit indiquer Chris doit à Alex
        const debiteur = page.getByTestId('balance-debiteur');
        await expect(debiteur).toBeVisible();
        await expect(debiteur).toContainText('Chris');
        await expect(debiteur).toContainText('Alex');
      });
    });

    test.describe('DASH-02: Détail complet de la balance', () => {
      test('dashboard shows total_chris, total_alex, balance_mensuelle, balance_reportee', async ({ page }) => {
        await seedDatabase(SCENARIO_SIMPLE);
        await loginAs(page, 'chris');
        await page.goto('/');

        // total_chris = 100€
        await expect(page.getByTestId('total-chris')).toContainText('100');
        // total_alex = 200€
        await expect(page.getByTestId('total-alex')).toContainText('200');
        // balance_mensuelle = +50€ (positif — Chris doit compenser)
        await expect(page.getByTestId('balance-mensuelle')).toContainText('50');
        // balance_reportee = 0 (first month — no previous)
        const balanceReportee = page.getByTestId('balance-reportee');
        await expect(balanceReportee).toBeVisible();
        // 0 s'affiche soit comme "0,00 €" ou "+ 0,00 €" — vérifier la présence de l'élément
        await expect(balanceReportee).toContainText('0');
      });
    });

    test.describe('DASH-03: Ventilation par catégorie', () => {
      test('dashboard shows per-category rows with amounts', async ({ page }) => {
        await seedDatabase(SCENARIO_SIMPLE);
        await loginAs(page, 'chris');
        await page.goto('/');

        // Alimentation: Chris 100 + Alex 200 = 300€ total
        const alimentation = page.getByTestId('synthese-alimentation');
        await expect(alimentation).toBeVisible();
        await expect(alimentation).toContainText('300');

        // Les 3 autres catégories existent mais affichent 0 (ou sont présentes)
        await expect(page.getByTestId('synthese-habitation')).toBeVisible();
        await expect(page.getByTestId('synthese-loisirs')).toBeVisible();
        await expect(page.getByTestId('synthese-vie_quotidienne')).toBeVisible();
      });
    });

    test.describe('DASH-04: Pas de cache', () => {
      test('fresh data on every load — no stale cache', async ({ page }) => {
        await seedDatabase(SCENARIO_SIMPLE);
        await loginAs(page, 'chris');

        // Premier chargement
        await page.goto('/');
        const firstBalance = await page.getByTestId('balance-finale').textContent();

        // Rechargement (force-dynamic = toujours fresh)
        await page.reload();
        const secondBalance = await page.getByTestId('balance-finale').textContent();

        // Les deux valeurs doivent être identiques (même données, pas de cache fluke)
        expect(firstBalance).toBe(secondBalance);
        expect(firstBalance).toContain('50');
      });
    });
    ```

    Note importante sur SCENARIO_SIMPLE et seedDatabase: le scénario passe `{ annee: 2026, mois: 3 }` dans les dépenses (pas mois_id). C'est le format attendu par seedDatabase (voir Plan 01 Task 2 — résolution d'ID interne). Si l'implémentation de seedDatabase utilise mois_id au lieu de annee/mois dans les dépenses, adapter les données de test pour correspondre à l'implémentation réelle.

    Vérifier l'interface SeedDepense exportée par tests/helpers/seed.ts avant d'écrire les données de test. Si elle utilise mois_id: number, utiliser mois_id avec une valeur connue (ou adapter seedDatabase pour retourner les IDs insérés).
  </action>
  <verify>
    <automated>npx playwright test tests/balance.spec.ts --grep "DASH-01|DASH-02|DASH-03|DASH-04" 2>&1 | tail -15</automated>
  </verify>
  <done>
    - DASH-01, DASH-02, DASH-03, DASH-04 : tests verts (passed)
    - Pas de test skipped pour ces 4 blocs
    - Les assertions utilisent les bons data-testid
  </done>
</task>

<task type="auto">
  <name>Task 2: Test RPT-01 et suite complète</name>
  <files>tests/balance.spec.ts</files>
  <action>
    Ajouter le test RPT-01 (report automatique) dans tests/balance.spec.ts et s'assurer que la suite complète passe.

    Le test RPT-01 vérifie que quand getOrCreateCurrentMois() crée un nouveau mois, sa balance_reportee est la balance_finale du mois précédent.

    Stratégie : seeder un mois passé (février 2026) avec des dépenses connues, puis visiter le dashboard en mars 2026 (= aujourd'hui dans l'environnement de test). Comme le mois de mars n'existe pas en DB après le seed de février, getOrCreateCurrentMois() doit le créer avec balance_reportee = balance_finale de février.

    ```typescript
    test.describe('RPT-01: Report automatique de la balance', () => {
      test('new month gets balance_reportee from previous month finale', async ({ page }) => {
        // Seeder UNIQUEMENT février 2026 avec des dépenses connues
        // Chris paie 100€, Alex paie 0€ → balance_mensuelle = (0-100)/2 = -50
        // balance_reportee = 0 (premier mois), Pas d'ajustements
        // balance_finale = -50 + 0 + 0 = -50 → Alex doit 50 à Chris
        const SCENARIO_FEV = {
          mois: [{ annee: 2026, mois: 2, balance_reportee: 0 }],
          depenses: [
            {
              annee: 2026, mois: 2,
              categorie: 'alimentation', sous_categorie: 'marche_pain',
              paye_par: 'chris' as const, montant: 100,
              label: 'courses fev', date_depense: '2026-02-01',
            },
          ],
        };

        await seedDatabase(SCENARIO_FEV);
        // Mars 2026 n'existe PAS en DB à ce stade

        await loginAs(page, 'chris');
        await page.goto('/');
        // La page charge mars 2026 (mois courant = 2026-03-05)
        // getOrCreateCurrentMois() doit créer mars 2026 avec balance_reportee = -50

        // Le report du mois précédent doit refléter -50
        const balanceReportee = page.getByTestId('balance-reportee');
        await expect(balanceReportee).toBeVisible();
        await expect(balanceReportee).toContainText('50'); // valeur absolue 50

        // Vérifier que la page affiche bien "mars" (mois courant)
        // Le titre du mois courant est "mars 2026" (date-fns/fr)
        await expect(page.getByText(/mars 2026/i)).toBeVisible();
      });
    });
    ```

    Après avoir ajouté RPT-01, vérifier que toute la suite passe :

    ```bash
    npx playwright test tests/balance.spec.ts
    ```

    Si un test échoue à cause d'un problème de date (l'environnement n'est pas en mars 2026), adapter RPT-01 pour utiliser le mois précédent réel (calculé depuis `new Date()`). Exemple:
    ```typescript
    const now = new Date();
    const currentMonth = now.getMonth() + 1; // 1-12
    const currentYear = now.getFullYear();
    const prevMonth = currentMonth === 1 ? 12 : currentMonth - 1;
    const prevYear = currentMonth === 1 ? currentYear - 1 : currentYear;
    // Seeder prevYear/prevMonth, vérifier que currentYear/currentMonth est créé avec le bon report
    ```

    Lancer la suite complète finale :
    ```bash
    npx playwright test
    ```
    Les auth tests doivent toujours passer. Les balance tests doivent tous passer.
  </action>
  <verify>
    <automated>npx playwright test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - npx playwright test : 0 failures, tous les tests passent (auth + balance)
    - RPT-01 : test green
    - Aucun test.todo() restant dans tests/balance.spec.ts
    - La suite complète se termine en < 60 secondes
  </done>
</task>

</tasks>

<verification>
- npx playwright test — 0 failures, suite complète verte
- npx playwright test tests/balance.spec.ts — tous les 5 blocs (DASH-01 à RPT-01) passent
- Les auth tests (auth.spec.ts) sont toujours verts
</verification>

<success_criteria>
- Suite Playwright complète : 0 failures
- DASH-01: dashboard affiche balance_finale et débiteur correct
- DASH-02: total_chris, total_alex, balance_mensuelle, balance_reportee tous présents et corrects
- DASH-03: 4 catégories visibles avec les bons montants
- DASH-04: rechargement retourne les mêmes données fraîches
- RPT-01: mois créé automatiquement avec le bon balance_reportee depuis le mois précédent
- Phase 2 prête pour /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/02-balance/02-04-SUMMARY.md`
</output>
