---
phase: 02-balance
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - app/(app)/page.tsx
  - app/(app)/loading.tsx
  - components/balance/BalanceCard.tsx
  - components/balance/BalanceSynthese.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04]

must_haves:
  truths:
    - "La page / affiche 'Chris doit X € à Alex' ou 'Alex doit X € à Chris' selon la balance"
    - "La page / affiche total_chris, total_alex, balance_mensuelle, balance_reportee (avec data-testid)"
    - "La page / affiche la ventilation par catégorie (4 lignes avec emoji)"
    - "export const dynamic = 'force-dynamic' est présent — pas de cache Next.js"
    - "Un état de chargement skeleton s'affiche pendant le rendu RSC"
    - "La page est mobile-first (touch targets 48px, police 16px min)"
  artifacts:
    - path: "app/(app)/page.tsx"
      provides: "Dashboard RSC complet"
      contains: "force-dynamic"
      exports: ["default (async function HomePage)"]
    - path: "app/(app)/loading.tsx"
      provides: "Loading skeleton state"
    - path: "components/balance/BalanceCard.tsx"
      provides: "Carte hero balance"
      contains: "data-testid"
    - path: "components/balance/BalanceSynthese.tsx"
      provides: "Tableau par catégorie"
      contains: "synthese-"
  key_links:
    - from: "app/(app)/page.tsx"
      to: "lib/db/queries/mois.ts"
      via: "import { getOrCreateCurrentMois }"
    - from: "app/(app)/page.tsx"
      to: "lib/balance.ts"
      via: "import { calculerBalance }"
    - from: "app/(app)/page.tsx"
      to: "components/balance/BalanceCard.tsx"
      via: "<BalanceCard balance={balance} mois={moisCourant} />"
    - from: "app/(app)/page.tsx"
      to: "components/balance/BalanceSynthese.tsx"
      via: "<BalanceSynthese depenses={depenses} />"
---

<objective>
Remplacer le stub du dashboard par la page RSC complète avec les composants d'affichage.

Purpose: C'est la livraison visible de la phase 2 — l'utilisateur voit enfin la balance dès la connexion. Tous les data-testid sont en place pour que Plan 04 puisse valider via Playwright.
Output: Dashboard fonctionnel mobile-first, BalanceCard hero, BalanceSynthese catégories, loading skeleton.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-balance/02-RESEARCH.md
@app/(app)/page.tsx
@lib/categories.ts
@.planning/phases/02-balance/02-02-SUMMARY.md

<interfaces>
<!-- Contrats fournis par Plan 02 -->
```typescript
// lib/balance.ts
export interface BalanceResult {
  total_chris: number;
  total_alex: number;
  balance_mensuelle: number;
  balance_reportee: number;
  total_chris_vers_alex: number;
  total_alex_vers_chris: number;
  balance_finale: number;
  // > 0: Chris doit à Alex  |  < 0: Alex doit à Chris  |  = 0: équilibre
}
export function calculerBalance(depenses: Depense[], ajustements: Ajustement[], balance_reportee: number): BalanceResult;

// lib/db/queries/mois.ts
export async function getOrCreateCurrentMois(): Promise<Mois>;

// lib/db/queries/depenses.ts
export async function getDepensesByMois(moisId: number): Promise<Depense[]>;

// lib/db/queries/ajustements.ts
export async function getAjustementsByMois(moisId: number): Promise<Ajustement[]>;

// types/index.ts
export type Mois = { id: number; annee: number; mois: number; balance_reportee: number; cree_le: Date };
export type Depense = { id: number; mois_id: number; categorie: string; sous_categorie: string; paye_par: string; montant: number; label: string | null; date_depense: Date; cree_le: Date };
export type Ajustement = { id: number; mois_id: number; de: string; vers: string; montant: number; label: string; date_ajustement: Date; cree_le: Date };

// lib/categories.ts (existant)
export const CATEGORIES = {
  alimentation: { label: 'Alimentation', emoji: '🛒', sous_categories: [...] },
  habitation:   { label: 'Habitation',   emoji: '🏠', sous_categories: [...] },
  loisirs:      { label: 'Loisirs',      emoji: '🎿', sous_categories: [...] },
  vie_quotidienne: { label: 'Vie quotidienne', emoji: '💊', sous_categories: [...] },
} as const;
export type Categorie = keyof typeof CATEGORIES;

// shadcn/ui disponibles: Card, CardContent, CardHeader, CardTitle, Badge, Separator, Skeleton
// Import pattern: import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
//                 import { Badge } from '@/components/ui/badge';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composants BalanceCard et BalanceSynthese</name>
  <files>components/balance/BalanceCard.tsx, components/balance/BalanceSynthese.tsx</files>
  <action>
    Créer components/balance/ avec les deux composants présentationnels. Ils reçoivent des props typées — zéro fetch, zéro 'use client'.

    **components/balance/BalanceCard.tsx**

    Props: `{ balance: BalanceResult; mois: Mois }`

    Affiche :
    - En-tête: le mois en français ("mars 2026") via `format(new Date(mois.annee, mois.mois - 1, 1), 'MMMM yyyy', { locale: fr })`
    - Carte hero (shadcn Card): message principal selon balance_finale:
      - balance_finale > 0: "Chris doit {montant} à Alex" (Badge variant="destructive" ou couleur warning)
      - balance_finale < 0: "Alex doit {montant} à Chris" (Badge en info/blue)
      - balance_finale === 0: "Équilibre" (Badge variant="outline")
      - data-testid="balance-finale" sur l'élément montant
      - data-testid="balance-debiteur" sur le label qui dit "Chris doit..." ou "Alex doit..."
    - Section détail (sous la carte hero):
      - data-testid="total-chris": total_chris formaté en EUR
      - data-testid="total-alex": total_alex formaté en EUR
      - data-testid="balance-mensuelle": balance_mensuelle formaté en EUR (avec signe)
      - data-testid="balance-reportee": balance_reportee formaté en EUR (avec signe, "Report mois préc.")
    - Formatage monétaire: `Math.abs(amount).toLocaleString('fr-FR', { style: 'currency', currency: 'EUR' })`
    - Pour balance_mensuelle et balance_reportee: afficher le signe (+ ou -) et la valeur absolue
    - Touch targets min 48px, texte min 16px (mobile-first CLAUDE.md)

    ```tsx
    // Structure approximative — adapter le style selon le design system shadcn existant
    import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
    import { Badge } from '@/components/ui/badge';
    import { Separator } from '@/components/ui/separator';
    import { format } from 'date-fns';
    import { fr } from 'date-fns/locale';
    import type { BalanceResult } from '@/lib/balance';
    import type { Mois } from '@/types';

    function formatEur(amount: number): string {
      return Math.abs(amount).toLocaleString('fr-FR', { style: 'currency', currency: 'EUR' });
    }

    function formatSigned(amount: number): string {
      return (amount >= 0 ? '+' : '-') + ' ' + formatEur(amount);
    }

    interface Props {
      balance: BalanceResult;
      mois: Mois;
    }

    export function BalanceCard({ balance, mois }: Props) {
      const { balance_finale, total_chris, total_alex, balance_mensuelle, balance_reportee } = balance;
      const moisLabel = format(new Date(mois.annee, mois.mois - 1, 1), 'MMMM yyyy', { locale: fr });

      // Débiteur
      let debiteurText: string;
      let badgeVariant: 'destructive' | 'default' | 'outline' | 'secondary';
      if (balance_finale > 0) {
        debiteurText = `Chris doit ${formatEur(balance_finale)} à Alex`;
        badgeVariant = 'destructive';
      } else if (balance_finale < 0) {
        debiteurText = `Alex doit ${formatEur(balance_finale)} à Chris`;
        badgeVariant = 'default';
      } else {
        debiteurText = 'Équilibre';
        badgeVariant = 'outline';
      }

      return (
        <div className="space-y-4">
          <p className="text-sm text-muted-foreground capitalize">{moisLabel}</p>

          <Card>
            <CardContent className="pt-6 pb-6 text-center space-y-3">
              <Badge variant={badgeVariant} className="text-base px-4 py-2 min-h-[48px] flex items-center justify-center">
                <span data-testid="balance-debiteur">{debiteurText}</span>
              </Badge>
              <p className="text-3xl font-bold" data-testid="balance-finale">
                {formatEur(balance_finale)}
              </p>
            </CardContent>
          </Card>

          <div className="grid grid-cols-2 gap-3 text-base">
            <div className="bg-muted rounded-lg p-3">
              <p className="text-xs text-muted-foreground mb-1">Chris</p>
              <p className="font-semibold" data-testid="total-chris">{formatEur(total_chris)}</p>
            </div>
            <div className="bg-muted rounded-lg p-3">
              <p className="text-xs text-muted-foreground mb-1">Alex</p>
              <p className="font-semibold" data-testid="total-alex">{formatEur(total_alex)}</p>
            </div>
          </div>

          <div className="text-base space-y-2">
            <div className="flex justify-between items-center min-h-[44px]">
              <span className="text-muted-foreground">Balance mensuelle</span>
              <span data-testid="balance-mensuelle">{formatSigned(balance_mensuelle)}</span>
            </div>
            <Separator />
            <div className="flex justify-between items-center min-h-[44px]">
              <span className="text-muted-foreground">Report mois préc.</span>
              <span data-testid="balance-reportee">{formatSigned(balance_reportee)}</span>
            </div>
          </div>
        </div>
      );
    }
    ```

    **components/balance/BalanceSynthese.tsx**

    Props: `{ depenses: Depense[] }`

    Affiche les 4 catégories (toujours, même si montant = 0) avec :
    - Emoji + label catégorie
    - Montant total catégorie formaté en EUR
    - data-testid={`synthese-${categorie}`} sur chaque ligne de catégorie

    ```tsx
    import { CATEGORIES, type Categorie } from '@/lib/categories';
    import type { Depense } from '@/types';

    function formatEur(amount: number): string {
      return amount.toLocaleString('fr-FR', { style: 'currency', currency: 'EUR' });
    }

    interface Props {
      depenses: Depense[];
    }

    export function BalanceSynthese({ depenses }: Props) {
      const categories = (Object.keys(CATEGORIES) as Categorie[]).map((cat) => {
        const catDepenses = depenses.filter((d) => d.categorie === cat);
        const total = catDepenses.reduce((sum, d) => sum + d.montant, 0);
        const chris = catDepenses.filter((d) => d.paye_par === 'chris').reduce((s, d) => s + d.montant, 0);
        const alex  = catDepenses.filter((d) => d.paye_par === 'alex').reduce((s, d) => s + d.montant, 0);
        return { cat, label: CATEGORIES[cat].label, emoji: CATEGORIES[cat].emoji, total, chris, alex };
      });

      if (categories.every((c) => c.total === 0)) {
        return (
          <p className="text-muted-foreground text-base text-center py-4">
            Aucune dépense ce mois
          </p>
        );
      }

      return (
        <div className="space-y-2">
          <h2 className="text-sm font-semibold text-muted-foreground uppercase tracking-wide">
            Par catégorie
          </h2>
          {categories.map(({ cat, label, emoji, total }) => (
            <div
              key={cat}
              className="flex justify-between items-center min-h-[48px] py-2"
              data-testid={`synthese-${cat}`}
            >
              <span className="text-base">
                {emoji} {label}
              </span>
              <span className="text-base font-medium">{formatEur(total)}</span>
            </div>
          ))}
        </div>
      );
    }
    ```

    Note Tailwind 4: utiliser les classes utilitaires standard. Pas de `tailwind.config.js` — la config est en CSS. Les classes grid, flex, rounded, bg-muted, text-muted-foreground fonctionnent comme en Tailwind 3.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "components/balance" | head -10 || echo "no TS errors in components"</automated>
  </verify>
  <done>
    - components/balance/BalanceCard.tsx existe avec data-testid balance-finale, balance-debiteur, total-chris, total-alex, balance-mensuelle, balance-reportee
    - components/balance/BalanceSynthese.tsx existe avec data-testid synthese-{categorie} pour les 4 catégories
    - Aucun 'use client' sur ces composants (RSC purs)
    - Pas d'erreur TypeScript
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard page.tsx et loading.tsx</name>
  <files>app/(app)/page.tsx, app/(app)/loading.tsx</files>
  <action>
    **app/(app)/page.tsx** — Remplacer le stub par le dashboard complet.

    ```tsx
    import { auth } from '@/lib/auth/index';
    import { getOrCreateCurrentMois } from '@/lib/db/queries/mois';
    import { getDepensesByMois } from '@/lib/db/queries/depenses';
    import { getAjustementsByMois } from '@/lib/db/queries/ajustements';
    import { calculerBalance } from '@/lib/balance';
    import { BalanceCard } from '@/components/balance/BalanceCard';
    import { BalanceSynthese } from '@/components/balance/BalanceSynthese';

    // DASH-04: jamais de cache — la balance doit être fraîche à chaque requête
    export const dynamic = 'force-dynamic';

    export default async function HomePage() {
      const session = await auth();
      const moisCourant = await getOrCreateCurrentMois();
      const depenses = await getDepensesByMois(moisCourant.id);
      const ajustements = await getAjustementsByMois(moisCourant.id);
      const balance = calculerBalance(depenses, ajustements, moisCourant.balance_reportee);

      return (
        <main className="p-4 space-y-6 pb-20">
          <header className="flex items-center justify-between">
            <h1 className="text-lg font-semibold">Le Commun</h1>
            <span className="text-sm text-muted-foreground">
              {session?.user?.id === 'chris' ? 'Chris' : 'Alex'}
            </span>
          </header>

          <BalanceCard balance={balance} mois={moisCourant} />

          <BalanceSynthese depenses={depenses} />
        </main>
      );
    }
    ```

    Note: pb-20 pour la bottom nav fixe existante (layout.tsx a déjà pb-16 ou similaire — vérifier et ajuster si nécessaire pour éviter le chevauchement).

    Note: Le signOut a été retiré du header (l'utilisateur se déconnecte via la nav ou un autre endroit — ne pas alourdir le dashboard). Si l'app (app)/layout.tsx n'a pas déjà de mécanisme de déconnexion, conserver un bouton Déconnexion discret dans le header, mais le déplacer si le layout le gère déjà.

    **app/(app)/loading.tsx** — État de chargement skeleton.

    ```tsx
    import { Skeleton } from '@/components/ui/skeleton';

    export default function Loading() {
      return (
        <main className="p-4 space-y-6 pb-20">
          <header className="flex items-center justify-between">
            <Skeleton className="h-6 w-32" />
            <Skeleton className="h-4 w-16" />
          </header>

          {/* Skeleton BalanceCard */}
          <div className="space-y-4">
            <Skeleton className="h-4 w-24" />
            <Skeleton className="h-32 w-full rounded-xl" />
            <div className="grid grid-cols-2 gap-3">
              <Skeleton className="h-16 rounded-lg" />
              <Skeleton className="h-16 rounded-lg" />
            </div>
            <Skeleton className="h-10 w-full" />
            <Skeleton className="h-10 w-full" />
          </div>

          {/* Skeleton BalanceSynthese */}
          <div className="space-y-2">
            <Skeleton className="h-4 w-28" />
            {[1, 2, 3, 4].map((i) => (
              <Skeleton key={i} className="h-12 w-full" />
            ))}
          </div>
        </main>
      );
    }
    ```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "app/\(app\)" | head -10 || echo "no TS errors in pages"</automated>
  </verify>
  <done>
    - app/(app)/page.tsx : RSC complet avec export const dynamic = 'force-dynamic'
    - app/(app)/page.tsx : importe et utilise BalanceCard et BalanceSynthese
    - app/(app)/loading.tsx : skeleton avec les bons composants shadcn
    - npx tsc --noEmit — 0 erreurs
    - npm run build passe sans erreur
  </done>
</task>

</tasks>

<verification>
- npm run build — build de production sans erreur
- npx tsc --noEmit — 0 erreurs TypeScript
- npm run dev puis curl http://localhost:3000/ — page HTML contient data-testid="balance-finale"
- npx playwright test tests/balance.spec.ts — 0 failures (stubs toujours skipped)
</verification>

<success_criteria>
- La page / se charge et affiche la balance du mois courant (DASH-01)
- Les data-testid requis sont présents : balance-finale, balance-debiteur, total-chris, total-alex, balance-mensuelle, balance-reportee, synthese-{4 categories}
- export const dynamic = 'force-dynamic' est présent (DASH-04)
- Aucun 'use client' sur les composants présentationnels
- npm run build sans erreur
</success_criteria>

<output>
After completion, create `.planning/phases/02-balance/02-03-SUMMARY.md`
</output>
