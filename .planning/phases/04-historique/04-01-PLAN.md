---
phase: 04-historique
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/historique.spec.ts
autonomous: true
requirements: [HIS-01, HIS-02]

must_haves:
  truths:
    - "npx playwright test tests/historique.spec.ts runs without syntax errors (all tests are fixme stubs)"
    - "HIS-01 has a named test block covering the month list page"
    - "HIS-02 has a named test block covering the archive detail page"
  artifacts:
    - path: "tests/historique.spec.ts"
      provides: "Stub test file with 2 fixme blocks matching HIS-01 and HIS-02"
  key_links:
    - from: "tests/historique.spec.ts"
      to: "tests/helpers/seed.ts"
      via: "import { seedDatabase } from './helpers/seed'"
      pattern: "seedDatabase"
    - from: "tests/historique.spec.ts"
      to: "tests/helpers/auth.ts"
      via: "import { loginAs } from './helpers/auth'"
      pattern: "loginAs"
---

<objective>
Create the Playwright test stub file for Phase 4 so that Nyquist is satisfied before any production code is written: every requirement (HIS-01, HIS-02) has a named test with a clear expectation. Plan 02 will make these tests green.

Purpose: Prevent the "no automated verify" anti-pattern. Both requirement IDs must have a test stub before the implementation begins.
Output: tests/historique.spec.ts — runnable with all tests marked fixme.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing test infrastructure — use these patterns exactly -->

From tests/helpers/auth.ts:
```typescript
export async function loginAs(page: Page, user: 'chris' | 'alex'): Promise<void>
```

From tests/helpers/seed.ts:
```typescript
export interface SeedData {
  mois: SeedMois[];         // { annee, mois, balance_reportee }
  depenses?: SeedDepense[]; // { annee, mois, categorie, sous_categorie, paye_par, montant, label?, date_depense }
  ajustements?: SeedAjustement[]; // { annee, mois, de, vers, montant, label, date_ajustement }
}
export async function seedDatabase(data: SeedData): Promise<void>
```

From tests/balance.spec.ts (test structure to follow):
```typescript
import { test, expect } from '@playwright/test';
import { loginAs } from './helpers/auth';
import { seedDatabase } from './helpers/seed';

test.describe('DASH-01: [description]', () => {
  test('[test name]', async ({ page }) => {
    await seedDatabase(SCENARIO);
    await loginAs(page, 'chris');
    // assertions
  });
});
```

Playwright note: use `test.fixme(true, 'reason')` for stubs — test.todo() is absent in Playwright 1.58.2.
Use `test.fixme(true, msg)` as the FIRST statement inside the test callback.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub historique.spec.ts with HIS-01 and HIS-02</name>
  <files>tests/historique.spec.ts</files>
  <action>
Create tests/historique.spec.ts. Import { test, expect } from '@playwright/test', { loginAs } from './helpers/auth', { seedDatabase } from './helpers/seed'.

Define a shared seed constant NOV_2024 with:
```typescript
const NOV_2024 = {
  mois: [{ annee: 2024, mois: 11, balance_reportee: 0 }],
  depenses: [
    { annee: 2024, mois: 11, categorie: 'alimentation', sous_categorie: 'marche_pain', paye_par: 'chris' as const, montant: 50, date_depense: '2024-11-01' },
    { annee: 2024, mois: 11, categorie: 'habitation', sous_categorie: 'loyer', paye_par: 'alex' as const, montant: 200, date_depense: '2024-11-05' },
  ],
  ajustements: [
    { annee: 2024, mois: 11, de: 'chris' as const, vers: 'alex' as const, montant: 100, label: 'virement test', date_ajustement: '2024-11-10' },
  ],
};
```

Create 2 test.describe blocks:

- test.describe('HIS-01: Liste des mois archivés', () => {
    test('la page /historique liste tous les mois avec leur balance finale', async ({ page }) => {
      test.fixme(true, 'not implemented — will be green after Plan 02');
    });
  });

- test.describe('HIS-02: Détail d\'un mois archivé', () => {
    test('/historique/[id] affiche les dépenses, ajustements et balance du mois archivé', async ({ page }) => {
      test.fixme(true, 'not implemented — will be green after Plan 02');
    });
  });

The fixme call must be the very first statement in each test body. No other code after test.fixme().
  </action>
  <verify>
    <automated>cd /home/chris/workspace/lecommun && npx playwright test tests/historique.spec.ts --project=chromium 2>&1 | tail -10</automated>
  </verify>
  <done>npx playwright test tests/historique.spec.ts runs, shows 2 fixme/skipped tests, zero failures, zero errors</done>
</task>

</tasks>

<verification>
Test file exists and Playwright runs it without errors:
```bash
cd /home/chris/workspace/lecommun && npx playwright test tests/historique.spec.ts --project=chromium 2>&1 | tail -10
```
Expected output: 2 skipped (fixme), 0 failed.
</verification>

<success_criteria>
- tests/historique.spec.ts exists with 2 named fixme stubs (HIS-01, HIS-02)
- npx playwright test tests/historique.spec.ts shows 0 failures
- Both requirement IDs for Phase 4 have a named test block
</success_criteria>

<output>
After completion, create `.planning/phases/04-historique/04-01-SUMMARY.md`
</output>
