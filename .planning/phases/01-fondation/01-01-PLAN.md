---
phase: 01-fondation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - drizzle.config.ts
  - lib/db/schema.ts
  - lib/db/index.ts
  - lib/auth/config.ts
  - lib/auth/index.ts
  - lib/categories.ts
  - playwright.config.ts
  - tests/auth.spec.ts
autonomous: true
requirements:
  - INF-02
  - INF-03
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Le projet Next.js 15 compile sans erreur (npm run build)"
    - "Le schéma DB Drizzle définit les 3 tables (mois, depenses, ajustements)"
    - "La config auth NextAuth v5 est Edge-compatible (lib/auth/config.ts sans import DB)"
    - "Les tests Playwright ont une infrastructure prête (playwright.config.ts + tests/auth.spec.ts)"
  artifacts:
    - path: "lib/db/schema.ts"
      provides: "3 tables Drizzle — mois, depenses, ajustements"
      contains: "sqliteTable.*mois.*depenses.*ajustements"
    - path: "lib/auth/config.ts"
      provides: "Config NextAuth Edge-safe (pas d'import DB)"
      exports: ["authConfig"]
    - path: "lib/auth/index.ts"
      provides: "Exports NextAuth — handlers, signIn, signOut, auth"
      exports: ["handlers", "signIn", "signOut", "auth"]
    - path: "playwright.config.ts"
      provides: "Config Playwright avec webServer localhost:3000"
    - path: "tests/auth.spec.ts"
      provides: "Stubs de tests E2E pour AUTH-01, AUTH-02, AUTH-03"
  key_links:
    - from: "lib/auth/config.ts"
      to: "middleware.ts (Plan 02)"
      via: "import authConfig"
      pattern: "authConfig.*NextAuthConfig"
    - from: "lib/db/schema.ts"
      to: "lib/db/index.ts"
      via: "import * as schema"
      pattern: "drizzle\\(client.*schema\\)"
---

<objective>
Bootstrapper le projet Next.js 15 et créer tous les fichiers de fondation : dépendances, schéma DB, configuration auth NextAuth v5 Edge-compatible, et infrastructure de tests Playwright.

Purpose: Ce plan établit la base sur laquelle tous les autres plans s'appuient. Sans un projet compilable, un schéma DB correct, et une config auth Edge-safe, rien ne peut fonctionner sur Vercel.

Output: Projet Next.js compilable avec les couches DB, auth, et tests prêtes — sans encore connecter à des services externes.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-fondation/01-CONTEXT.md
@.planning/phases/01-fondation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap projet Next.js 15 et installer les dépendances</name>
  <files>package.json, tsconfig.json, next.config.ts, tailwind.config.ts, drizzle.config.ts, lib/categories.ts</files>
  <action>
Exécuter depuis /home/chris/workspace/lecommun :

1. Créer le projet Next.js 15 avec TypeScript, Tailwind, App Router, ESLint :
```bash
npx create-next-app@latest . --typescript --tailwind --app --eslint --src-dir=false --import-alias="@/*"
```
Si des fichiers existent déjà (CLAUDE.md, docs/), répondre "yes" pour continuer sans écraser.

2. Installer les dépendances core :
```bash
npm install next-auth@beta @libsql/client drizzle-orm zod date-fns lucide-react
npm install -D drizzle-kit
```

3. Initialiser shadcn/ui (choisir style "default", base color "slate", CSS variables "yes") :
```bash
npx shadcn@latest init --defaults
npx shadcn@latest add button card input label --legacy-peer-deps
```

4. Créer drizzle.config.ts à la racine :
```typescript
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  out: './drizzle',
  schema: './lib/db/schema.ts',
  dialect: 'turso',
  dbCredentials: {
    url: process.env.TURSO_DATABASE_URL!,
    authToken: process.env.TURSO_AUTH_TOKEN,
  },
});
```
IMPORTANT : dialect doit être 'turso' (pas 'sqlite') — cf. pitfall 6 de la RESEARCH.

5. Créer lib/categories.ts avec les constantes :
```typescript
export const CATEGORIES = {
  alimentation: {
    label: 'Alimentation',
    emoji: '🛒',
    sous_categories: ['marche_pain', 'intermarche', 'viande', 'miel'],
  },
  habitation: {
    label: 'Habitation',
    emoji: '🏠',
    sous_categories: ['loyer', 'energie', 'internet', 'eau', 'assurance', 'amazon', 'netflix'],
  },
  loisirs: {
    label: 'Loisirs',
    emoji: '🎿',
    sous_categories: ['restaurant', 'vacances', 'sport', 'livres'],
  },
  vie_quotidienne: {
    label: 'Vie quotidienne',
    emoji: '💊',
    sous_categories: ['pharmacie', 'animaux', 'cadeaux', 'appartement'],
  },
} as const;

export type Categorie = keyof typeof CATEGORIES;
export type PayePar = 'chris' | 'alex';
```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Le projet compile sans erreur TypeScript. Les dépendances sont installées (node_modules contient next-auth, drizzle-orm, @libsql/client, zod). drizzle.config.ts existe avec dialect: 'turso'. lib/categories.ts exporte CATEGORIES, Categorie, PayePar.</done>
</task>

<task type="auto">
  <name>Task 2: Créer le schéma DB et le client Drizzle</name>
  <files>lib/db/schema.ts, lib/db/index.ts</files>
  <action>
Créer lib/db/schema.ts avec les 3 tables (copie exacte du TECH_SPEC) :
```typescript
import { sqliteTable, text, integer, real } from 'drizzle-orm/sqlite-core';

export const mois = sqliteTable('mois', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  annee: integer('annee').notNull(),
  mois: integer('mois').notNull(),
  balance_reportee: real('balance_reportee').notNull().default(0),
  cree_le: integer('cree_le', { mode: 'timestamp' })
    .notNull().$defaultFn(() => new Date()),
});

export const depenses = sqliteTable('depenses', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  mois_id: integer('mois_id').notNull()
    .references(() => mois.id, { onDelete: 'cascade' }),
  categorie: text('categorie').notNull(),
  sous_categorie: text('sous_categorie').notNull(),
  paye_par: text('paye_par').notNull(),
  montant: real('montant').notNull(),
  label: text('label'),
  date_depense: integer('date_depense', { mode: 'timestamp' }).notNull(),
  cree_le: integer('cree_le', { mode: 'timestamp' })
    .notNull().$defaultFn(() => new Date()),
});

export const ajustements = sqliteTable('ajustements', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  mois_id: integer('mois_id').notNull()
    .references(() => mois.id, { onDelete: 'cascade' }),
  de: text('de').notNull(),
  vers: text('vers').notNull(),
  montant: real('montant').notNull(),
  label: text('label').notNull(),
  date_ajustement: integer('date_ajustement', { mode: 'timestamp' }).notNull(),
  cree_le: integer('cree_le', { mode: 'timestamp' })
    .notNull().$defaultFn(() => new Date()),
});
```

Créer lib/db/index.ts :
```typescript
import { createClient } from '@libsql/client';
import { drizzle } from 'drizzle-orm/libsql';
import * as schema from './schema';

const client = createClient({
  url: process.env.TURSO_DATABASE_URL!,
  authToken: process.env.TURSO_AUTH_TOKEN!,
});

export const db = drizzle(client, { schema });
```

Créer le dossier lib/db/queries/ (vide pour l'instant, sera peuplé en Phase 2) :
```bash
mkdir -p lib/db/queries
touch lib/db/queries/.gitkeep
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "error TS" | head -10 || echo "TypeScript OK"</automated>
  </verify>
  <done>lib/db/schema.ts exporte les tables mois, depenses, ajustements avec les types corrects. lib/db/index.ts exporte db. npx tsc --noEmit passe sans erreur sur ces fichiers.</done>
</task>

<task type="auto">
  <name>Task 3: Créer la configuration NextAuth v5 (split Edge-safe) et les stubs Playwright</name>
  <files>lib/auth/config.ts, lib/auth/index.ts, playwright.config.ts, tests/auth.spec.ts</files>
  <action>
CRITIQUE : La config auth est splitée en deux fichiers pour la compatibilité Edge de Vercel.
lib/auth/config.ts ne doit JAMAIS importer depuis lib/db/ ou toute lib Node.js-only.

Créer lib/auth/config.ts :
```typescript
import type { NextAuthConfig } from 'next-auth';
import Credentials from 'next-auth/providers/credentials';

export const authConfig: NextAuthConfig = {
  providers: [
    Credentials({
      credentials: {
        username: { label: 'Utilisateur', type: 'text' },
        password: { label: 'Mot de passe', type: 'password' },
      },
      async authorize(credentials) {
        const { username, password } = credentials as {
          username: string;
          password: string;
        };
        const users = [
          { id: 'chris', name: 'Chris', username: 'chris', password: process.env.CHRIS_PASSWORD! },
          { id: 'alex', name: 'Alex', username: 'alex', password: process.env.ALEX_PASSWORD! },
        ];
        const user = users.find(
          (u) => u.username === username && u.password === password
        );
        return user ?? null;
      },
    }),
  ],
  pages: { signIn: '/login' },
  session: { strategy: 'jwt', maxAge: 30 * 24 * 60 * 60 },
  callbacks: {
    jwt({ token, user }) {
      if (user) token.userId = user.id;
      return token;
    },
    session({ session, token }) {
      session.user.id = token.userId as string;
      return session;
    },
  },
};
```

Créer lib/auth/index.ts :
```typescript
import NextAuth from 'next-auth';
import { authConfig } from './config';

export const { handlers, signIn, signOut, auth } = NextAuth(authConfig);
```

Installer Playwright et créer playwright.config.ts :
```bash
npm init playwright@latest -- --quiet --browser=chromium --lang=TypeScript
```
Si le wizard demande des choix interactifs, utiliser les flags pour éviter l'interaction.
Alternativement, installer directement :
```bash
npm install -D @playwright/test
npx playwright install chromium
```

Créer playwright.config.ts :
```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests',
  fullyParallel: false,
  forbidOnly: !!process.env.CI,
  retries: 0,
  workers: 1,
  reporter: 'line',
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 120 * 1000,
  },
});
```

Créer tests/auth.spec.ts avec les stubs (tests qui ÉCHOUENT jusqu'à ce que Plan 02 soit exécuté — c'est voulu) :
```typescript
import { test, expect } from '@playwright/test';

const TEST_USERS = {
  chris: { username: 'chris', password: process.env.CHRIS_PASSWORD ?? 'test-chris' },
  alex: { username: 'alex', password: process.env.ALEX_PASSWORD ?? 'test-alex' },
};

test.describe('AUTH-01: Connexion chris et alex', () => {
  test('chris login — chris peut se connecter avec son mot de passe', async ({ page }) => {
    await page.goto('/login');
    await page.getByRole('button', { name: 'Chris' }).click();
    await page.getByLabel(/mot de passe/i).fill(TEST_USERS.chris.password);
    await page.getByRole('button', { name: /connexion/i }).click();
    await expect(page).toHaveURL('/');
  });

  test('alex login — alex peut se connecter avec son mot de passe', async ({ page }) => {
    await page.goto('/login');
    await page.getByRole('button', { name: 'Alex' }).click();
    await page.getByLabel(/mot de passe/i).fill(TEST_USERS.alex.password);
    await page.getByRole('button', { name: /connexion/i }).click();
    await expect(page).toHaveURL('/');
  });

  test('wrong password — mauvais mot de passe affiche "Identifiants incorrects"', async ({ page }) => {
    await page.goto('/login');
    await page.getByRole('button', { name: 'Chris' }).click();
    await page.getByLabel(/mot de passe/i).fill('mauvais-mot-de-passe');
    await page.getByRole('button', { name: /connexion/i }).click();
    await expect(page.getByText('Identifiants incorrects')).toBeVisible();
  });
});

test.describe('AUTH-02: Session 30 jours', () => {
  test('session persists — le cookie de session a une durée de 30 jours', async ({ page, context }) => {
    await page.goto('/login');
    await page.getByRole('button', { name: 'Chris' }).click();
    await page.getByLabel(/mot de passe/i).fill(TEST_USERS.chris.password);
    await page.getByRole('button', { name: /connexion/i }).click();
    await expect(page).toHaveURL('/');

    const cookies = await context.cookies();
    const sessionCookie = cookies.find((c) => c.name.includes('session-token') || c.name.includes('authjs'));
    expect(sessionCookie).toBeDefined();
    // maxAge ~30 jours = 2592000s; tolérance de 60s pour le timing du test
    if (sessionCookie?.expires) {
      const maxAgeSeconds = sessionCookie.expires - Date.now() / 1000;
      expect(maxAgeSeconds).toBeGreaterThan(2592000 - 60);
    }
  });
});

test.describe('AUTH-03: Protection des routes', () => {
  test('redirect unauthenticated — accès à / sans session redirige vers /login', async ({ page }) => {
    await page.goto('/');
    await expect(page).toHaveURL(/\/login/);
  });

  test('redirect authenticated — accès à /login avec session redirige vers /', async ({ page, context }) => {
    // Se connecter d'abord
    await page.goto('/login');
    await page.getByRole('button', { name: 'Chris' }).click();
    await page.getByLabel(/mot de passe/i).fill(TEST_USERS.chris.password);
    await page.getByRole('button', { name: /connexion/i }).click();
    await expect(page).toHaveURL('/');

    // Tenter d'accéder à /login avec la session active
    await page.goto('/login');
    await expect(page).toHaveURL('/');
  });
});
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "error TS" | head -10 || echo "TypeScript OK"</automated>
  </verify>
  <done>lib/auth/config.ts et lib/auth/index.ts existent et exportent les bons symboles. playwright.config.ts pointe sur localhost:3000. tests/auth.spec.ts contient les 6 tests (chris login, alex login, wrong password, session persists, redirect unauthenticated, redirect authenticated). TypeScript ne remonte pas d'erreur sur ces fichiers.</done>
</task>

</tasks>

<verification>
Après exécution du plan :
1. `npm run build` passe sans erreur
2. `npx tsc --noEmit` passe sans erreur
3. `lib/db/schema.ts` contient les 3 tables avec les bons champs
4. `lib/auth/config.ts` n'importe PAS depuis `lib/db/` (vérifier avec grep)
5. `playwright.config.ts` et `tests/auth.spec.ts` existent
6. Aucun fichier de Plan 01 ne fait d'appel réseau — tout est purement structurel
</verification>

<success_criteria>
- Le projet Next.js 15 compile sans erreur TypeScript
- lib/db/schema.ts définit exactement les 3 tables du TECH_SPEC
- lib/auth/config.ts est Edge-safe (zéro import DB ou Node.js-only)
- L'infrastructure Playwright est prête (config + stubs de tests)
- lib/categories.ts exporte les constantes de catégories
</success_criteria>

<output>
After completion, create `.planning/phases/01-fondation/01-01-SUMMARY.md`
</output>
