---
phase: 01-fondation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .env.local
  - drizzle/ (generated migrations)
autonomous: false
requirements:
  - INF-01
  - INF-02
  - INF-03
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Chris et Alex peuvent se connecter sur l'application en production Vercel"
    - "La DB Turso contient les 3 tables (mois, depenses, ajustements)"
    - "Le déploiement Vercel est automatique sur push main"
    - "Toutes les variables d'environnement sont configurées sur Vercel"
    - "Les tests Playwright AUTH-01/02/03 passent en vert"
  artifacts:
    - path: ".env.local"
      provides: "Variables d'environnement locales pour dev"
      contains: "TURSO_DATABASE_URL.*TURSO_AUTH_TOKEN.*AUTH_SECRET"
    - path: "drizzle/"
      provides: "Fichiers de migration générés"
      contains: "*.sql"
  key_links:
    - from: "Vercel project"
      to: "GitHub repo branch main"
      via: "CD automatique sur push"
      pattern: "CD auto-deploy"
    - from: "drizzle-kit migrate"
      to: "Turso DB"
      via: "TURSO_DATABASE_URL + TURSO_AUTH_TOKEN"
      pattern: "drizzle.*migrate.*turso"

user_setup:
  - service: turso
    why: "Base de données SQLite cloud pour les tables mois, depenses, ajustements"
    env_vars:
      - name: TURSO_DATABASE_URL
        source: "turso db show lecommun --url (après création DB)"
      - name: TURSO_AUTH_TOKEN
        source: "turso db tokens create lecommun"
    dashboard_config:
      - task: "Installer le CLI Turso et créer la DB"
        location: "Terminal local — voir instructions Task 1"
  - service: vercel
    why: "Hébergement et CD automatique sur push main"
    env_vars:
      - name: AUTH_SECRET
        source: "Généré localement — openssl rand -base64 32"
      - name: CHRIS_PASSWORD
        source: "Choisir un mot de passe pour Chris"
      - name: ALEX_PASSWORD
        source: "Choisir un mot de passe pour Alex"
    dashboard_config:
      - task: "Importer le projet GitHub dans Vercel et configurer les 5 env vars"
        location: "vercel.com/new — voir instructions Task 2"
---

<objective>
Provisionner l'infrastructure externe (Turso DB + Vercel), appliquer le schéma DB, configurer toutes les variables d'environnement, et valider le tout avec les tests Playwright.

Purpose: Ce plan connecte le code aux services réels — sans cette étape, l'app ne peut pas fonctionner (pas de DB, pas de sessions auth, pas de déploiement).

Output: Application déployée en production avec auth fonctionnelle, DB prête, et tests E2E verts.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-fondation/01-CONTEXT.md
@.planning/phases/01-fondation/01-01-SUMMARY.md
@.planning/phases/01-fondation/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provisionnement Turso DB et création du .env.local</name>
  <files>.env.local, drizzle/</files>
  <action>
ACTION REQUISE PAR L'UTILISATEUR — Claude ne peut pas accéder aux services externes.

Provisionner la base de données Turso et configurer l'environnement local.

Étape 1 — Installer le CLI Turso (si pas déjà fait) :
```bash
curl -sSfL https://get.tur.so/install.sh | bash
turso auth login
```

Étape 2 — Créer la base de données :
```bash
turso db create lecommun
```

Étape 3 — Récupérer les credentials :
```bash
turso db show lecommun --url
# Copier la valeur -> TURSO_DATABASE_URL (format: libsql://lecommun-xxx.turso.io)

turso db tokens create lecommun
# Copier la valeur -> TURSO_AUTH_TOKEN
```

Étape 4 — Générer AUTH_SECRET :
```bash
openssl rand -base64 32
# Copier la valeur -> AUTH_SECRET
```

Étape 5 — Créer .env.local à la racine du projet (NE PAS committer — déjà dans .gitignore) :
```
TURSO_DATABASE_URL=libsql://lecommun-[ton-org].turso.io
TURSO_AUTH_TOKEN=eyJhbGci...
AUTH_SECRET=[résultat openssl]
CHRIS_PASSWORD=[choisir un mot de passe pour Chris]
ALEX_PASSWORD=[choisir un mot de passe pour Alex]
```

Étape 6 — Appliquer le schéma à la DB :
```bash
npx drizzle-kit generate
npx drizzle-kit migrate
```

Vérification optionnelle :
```bash
npx drizzle-kit studio
# Ouvrir http://localhost:4983 — vérifier les tables mois, depenses, ajustements
```
  </action>
  <verify>MISSING — action humaine requise. Résultat confirmé par le signal de reprise.</verify>
  <done>.env.local existe avec les 5 variables. `npx drizzle-kit migrate` a réussi. Les tables mois, depenses, ajustements existent dans Turso.</done>
  <resume-signal>Tape "turso ok" une fois que .env.local est créé et que `npx drizzle-kit migrate` a réussi</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Déploiement Vercel et configuration des variables d'environnement</name>
  <files>vercel project (externe)</files>
  <action>
ACTION REQUISE PAR L'UTILISATEUR — Connexion Vercel/GitHub requise.

Connecter le projet à Vercel pour le CD automatique sur push main.

Étape 1 — S'assurer que le code est poussé sur GitHub (branche main) :
```bash
git init
git add -A
git commit -m "feat(01-fondation): fondation complète"
git remote add origin https://github.com/[ton-username]/lecommun.git
git push -u origin main
```
(Sauter si le repo est déjà sur GitHub.)

Étape 2 — Créer le projet Vercel :
1. Aller sur https://vercel.com/new
2. Cliquer "Import Git Repository"
3. Sélectionner le repo `lecommun`
4. NE PAS encore cliquer "Deploy" — configurer les env vars d'abord

Étape 3 — Configurer les 5 variables d'environnement dans Vercel (environnement "Production") :
- TURSO_DATABASE_URL = (valeur de .env.local)
- TURSO_AUTH_TOKEN = (valeur de .env.local)
- AUTH_SECRET = (valeur de .env.local)
- CHRIS_PASSWORD = (valeur choisie)
- ALEX_PASSWORD = (valeur choisie)

IMPORTANT : AUTH_SECRET est obligatoire — NextAuth lève "MissingSecret" sans lui.

Étape 4 — Déclencher le premier déploiement :
Cliquer "Deploy". Attendre que le build passe (~2-3 minutes).

Étape 5 — Vérifier :
- Le build Vercel passe sans erreur (logs dans dashboard)
- L'URL de production (ex: lecommun.vercel.app) charge /login
- Se connecter avec Chris ou Alex -> atterrir sur /
  </action>
  <verify>MISSING — action humaine requise. Résultat confirmé par le signal de reprise (URL Vercel fournie).</verify>
  <done>Projet Vercel créé, lié au repo GitHub (branche main). Les 5 env vars sont configurées. Le premier déploiement a réussi. L'URL de production charge /login.</done>
  <resume-signal>Tape "vercel ok [URL]" en remplaçant [URL] par l'URL de production Vercel (ex: "vercel ok https://lecommun.vercel.app")</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Validation E2E — tests Playwright AUTH-01/02/03</name>
  <files>tests/auth.spec.ts</files>
  <action>
Avec .env.local configuré et le dev server accessible sur localhost:3000, exécuter la suite complète de tests Playwright.

Les tests utilisent CHRIS_PASSWORD et ALEX_PASSWORD depuis .env.local automatiquement.

Lancer les tests :
```bash
npx playwright test tests/auth.spec.ts --project=chromium
```

Si des tests échouent, diagnostiquer par cas :
- "chris login" échoue : vérifier que CHRIS_PASSWORD dans .env.local correspond
- "redirect unauthenticated" échoue : vérifier que middleware.ts est à la racine du projet
- "session persists" cookie check échoue : NextAuth v5 peut nommer le cookie différemment. Inspecter les cookies :
  ```typescript
  const cookies = await context.cookies();
  console.log(cookies.map(c => c.name));
  ```
  Adapter le sélecteur `find()` dans tests/auth.spec.ts pour le bon nom de cookie.

Exécuter la suite complète après corrections éventuelles :
```bash
npx playwright test
```
  </action>
  <verify>
    <automated>npx playwright test tests/auth.spec.ts --project=chromium 2>&1 | tail -10</automated>
  </verify>
  <done>Les 6 tests passent en vert : chris login, alex login, wrong password, session persists, redirect unauthenticated, redirect authenticated. Aucun test rouge ou flaky.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Vérification visuelle mobile — app en production</name>
  <files>app en production Vercel</files>
  <action>
Vérification manuelle de l'application déployée sur mobile ou DevTools mode responsive.
  </action>
  <verify>
Tester sur mobile (iPhone 14 390x844) ou DevTools Chrome responsive mode :

1. Test déconnecté : Visiter l'URL Vercel -> page /login s'affiche
2. Login Chris : Cliquer "Chris" (bouton mis en évidence) -> saisir mot de passe -> cliquer Connexion -> atterrir sur /
3. Navigation : Cliquer chaque onglet de la bottom bar -> pages placeholder s'affichent, onglet actif mis en évidence
4. Déconnexion : Cliquer "Déconnexion" sur / -> retour sur /login
5. Protection : Accéder directement à /depenses sans session -> redirection vers /login
6. Mobile check : Boutons Chris/Alex >= 48px de hauteur. Champ mot de passe >= 16px de police (pas de zoom iOS).
  </verify>
  <done>L'application est visuellement correcte sur mobile : login fonctionnel, navigation opérationnelle, protection des routes vérifiée, touch targets conformes aux 48px minimum.</done>
  <resume-signal>Tape "approved" si tout fonctionne, ou décris les problèmes rencontrés pour correction</resume-signal>
</task>

</tasks>

<verification>
Phase 1 complète quand :
1. `npx playwright test tests/auth.spec.ts` passe en vert (6/6 tests)
2. L'URL Vercel charge /login sans erreur
3. Chris ET Alex peuvent se connecter en production
4. Push sur main déclenche un nouveau déploiement Vercel automatiquement
5. La DB Turso contient les 3 tables vides (visible via drizzle-kit studio)
</verification>

<success_criteria>
- INF-01 : CD Vercel actif, déploiement auto sur push main
- INF-02 : Tables mois, depenses, ajustements créées dans Turso
- INF-03 : Les 5 env vars configurées sur Vercel (TURSO_DATABASE_URL, TURSO_AUTH_TOKEN, AUTH_SECRET, CHRIS_PASSWORD, ALEX_PASSWORD)
- AUTH-01 : Chris et Alex peuvent se connecter, message "Identifiants incorrects" sur mauvais MDP
- AUTH-02 : Session persiste 30 jours (cookie maxAge)
- AUTH-03 : Toutes les routes sauf /login protégées par le middleware
</success_criteria>

<output>
After completion, create `.planning/phases/01-fondation/01-03-SUMMARY.md`
</output>
