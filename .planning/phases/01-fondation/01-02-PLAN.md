---
phase: 01-fondation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - middleware.ts
  - app/api/auth/[...nextauth]/route.ts
  - app/(auth)/login/page.tsx
  - app/(app)/layout.tsx
  - app/(app)/page.tsx
  - app/(app)/depenses/page.tsx
  - app/(app)/ajustements/page.tsx
  - app/(app)/historique/page.tsx
  - components/layout/BottomNav.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "La page /login affiche deux boutons segmentés (Chris / Alex) et un champ mot de passe"
    - "La connexion réussie redirige vers /"
    - "Un mauvais mot de passe affiche 'Identifiants incorrects'"
    - "Toute route sauf /login est protégée par le middleware"
    - "La bottom bar fixe est présente sur toutes les pages authentifiées"
  artifacts:
    - path: "middleware.ts"
      provides: "Protection de toutes les routes sauf /login"
      contains: "auth.*req"
    - path: "app/(auth)/login/page.tsx"
      provides: "Page de connexion avec boutons segmentés Chris/Alex"
      contains: "selectedUser.*chris.*alex"
    - path: "app/(app)/layout.tsx"
      provides: "Layout protégé avec BottomNav, padding bottom 64px"
      contains: "BottomNav"
    - path: "components/layout/BottomNav.tsx"
      provides: "Navigation bottom bar fixe 4 onglets, touch targets 48px"
      exports: ["BottomNav"]
  key_links:
    - from: "middleware.ts"
      to: "lib/auth/config.ts"
      via: "import { auth } from lib/auth/index"
      pattern: "auth.*lib/auth"
    - from: "app/(auth)/login/page.tsx"
      to: "next-auth/react"
      via: "signIn('credentials', ...)"
      pattern: "signIn.*credentials"
    - from: "app/(app)/layout.tsx"
      to: "components/layout/BottomNav.tsx"
      via: "import BottomNav"
      pattern: "BottomNav"

user_setup: []
---

<objective>
Construire toute l'interface utilisateur de Phase 1 : la page de connexion avec sélection segmentée Chris/Alex, le middleware de protection des routes, le layout app avec bottom navigation, et les pages placeholder pour les 4 sections.

Purpose: Ce plan transforme les fichiers de fondation de Plan 01 en une application navigable — les utilisateurs peuvent se connecter, naviguer entre les sections, et se déconnecter.

Output: Application Next.js fonctionnelle avec auth opérationnelle et navigation complète.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-fondation/01-CONTEXT.md
@.planning/phases/01-fondation/01-RESEARCH.md
@.planning/phases/01-fondation/01-01-SUMMARY.md

<interfaces>
<!-- Types et exports créés par Plan 01 — à utiliser directement -->

From lib/auth/config.ts:
```typescript
export const authConfig: NextAuthConfig;
// Contient: providers Credentials (username: 'chris'|'alex'), pages.signIn: '/login'
// session: { strategy: 'jwt', maxAge: 2592000 }
```

From lib/auth/index.ts:
```typescript
export const { handlers, signIn, signOut, auth } = NextAuth(authConfig);
// - handlers: GET/POST pour la route NextAuth
// - signIn: Server Action (import depuis lib/auth/index.ts)
// - signOut: Server Action
// - auth: fonction universelle RSC/Server Action/middleware
```

From lib/categories.ts:
```typescript
export type PayePar = 'chris' | 'alex';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Middleware de protection des routes et route handler NextAuth</name>
  <files>middleware.ts, app/api/auth/[...nextauth]/route.ts</files>
  <action>
Créer middleware.ts à la racine du projet (même niveau que package.json) :
```typescript
import { auth } from '@/lib/auth/index';

export default auth((req) => {
  const isLoggedIn = !!req.auth;
  const isAuthPage = req.nextUrl.pathname.startsWith('/login');

  if (!isLoggedIn && !isAuthPage) {
    return Response.redirect(new URL('/login', req.url));
  }
  if (isLoggedIn && isAuthPage) {
    return Response.redirect(new URL('/', req.url));
  }
});

export const config = {
  matcher: ['/((?!api/auth|_next/static|_next/image|favicon.ico).*)'],
};
```

IMPORTANT : Ce middleware importe depuis lib/auth/index (qui importe lib/auth/config.ts, Edge-safe).
NE PAS importer depuis lib/db/ dans ce fichier — cf. pitfall 1 de la RESEARCH.

Créer app/api/auth/[...nextauth]/route.ts :
```typescript
import { handlers } from '@/lib/auth/index';

export const { GET, POST } = handlers;
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "error TS" | head -10 || echo "TypeScript OK"</automated>
  </verify>
  <done>middleware.ts existe à la racine, importe depuis lib/auth/index, contient le matcher correct. app/api/auth/[...nextauth]/route.ts exporte GET et POST. TypeScript passe sans erreur.</done>
</task>

<task type="auto">
  <name>Task 2: Page de connexion avec boutons segmentés Chris/Alex</name>
  <files>app/(auth)/login/page.tsx</files>
  <action>
Créer app/(auth)/login/page.tsx — c'est un Client Component (interactivité requise).

Décisions locked (CONTEXT.md) :
- Boutons segmentés Chris/Alex (pas de champ texte pour le nom)
- Un seul champ : mot de passe
- Message d'erreur générique : "Identifiants incorrects"
- Design épuré shadcn/ui, centré verticalement

```typescript
'use client';

import { useState } from 'react';
import { signIn } from 'next-auth/react';
import { useRouter } from 'next/navigation';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function LoginPage() {
  const [selectedUser, setSelectedUser] = useState<'chris' | 'alex' | null>(null);
  const [password, setPassword] = useState('');
  const [error, setError] = useState('');
  const [loading, setLoading] = useState(false);
  const router = useRouter();

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    if (!selectedUser) return;
    setLoading(true);
    setError('');
    const result = await signIn('credentials', {
      username: selectedUser,
      password,
      redirect: false,
    });
    setLoading(false);
    if (result?.error) {
      setError('Identifiants incorrects');
    } else {
      router.push('/');
    }
  }

  return (
    <div className="min-h-screen flex items-center justify-center p-4 bg-background">
      <Card className="w-full max-w-sm">
        <CardHeader className="text-center">
          <CardTitle className="text-2xl">Le Commun</CardTitle>
        </CardHeader>
        <CardContent>
          <form onSubmit={handleSubmit} className="space-y-6">
            <div className="space-y-2">
              <Label>Qui es-tu ?</Label>
              <div className="grid grid-cols-2 gap-2">
                <Button
                  type="button"
                  variant={selectedUser === 'chris' ? 'default' : 'outline'}
                  className="h-12 text-base"
                  onClick={() => setSelectedUser('chris')}
                >
                  Chris
                </Button>
                <Button
                  type="button"
                  variant={selectedUser === 'alex' ? 'default' : 'outline'}
                  className="h-12 text-base"
                  onClick={() => setSelectedUser('alex')}
                >
                  Alex
                </Button>
              </div>
            </div>

            <div className="space-y-2">
              <Label htmlFor="password">Mot de passe</Label>
              <Input
                id="password"
                type="password"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                className="h-12 text-base"
                autoComplete="current-password"
                required
              />
            </div>

            {error && (
              <p className="text-sm text-destructive text-center">{error}</p>
            )}

            <Button
              type="submit"
              className="w-full h-12 text-base"
              disabled={!selectedUser || !password || loading}
            >
              {loading ? 'Connexion...' : 'Connexion'}
            </Button>
          </form>
        </CardContent>
      </Card>
    </div>
  );
}
```

Note sur le style :
- h-12 = 48px touch targets (règle mobile-first CLAUDE.md)
- text-base = 16px minimum (évite le zoom iOS)
- max-w-sm centré = lisible sur mobile 390px
- La discretion styling (couleurs, espacement exact) appartient à Claude — le design ci-dessus est épuré et fonctionnel
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "error TS" | head -10 || echo "TypeScript OK"</automated>
  </verify>
  <done>app/(auth)/login/page.tsx est un Client Component avec les boutons segmentés Chris/Alex, un champ mot de passe, la gestion d'erreur "Identifiants incorrects", et l'utilisation de signIn depuis next-auth/react (pas depuis lib/auth/index). Touch targets h-12 = 48px minimum.</done>
</task>

<task type="auto">
  <name>Task 3: Layout app protégé, BottomNav et pages placeholder</name>
  <files>app/(app)/layout.tsx, app/(app)/page.tsx, app/(app)/depenses/page.tsx, app/(app)/ajustements/page.tsx, app/(app)/historique/page.tsx, components/layout/BottomNav.tsx</files>
  <action>
Créer components/layout/BottomNav.tsx — Client Component (nécessite usePathname) :
```typescript
'use client';

import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { Home, Receipt, ArrowLeftRight, Clock } from 'lucide-react';

const links = [
  { href: '/', label: 'Accueil', icon: Home },
  { href: '/depenses', label: 'Dépenses', icon: Receipt },
  { href: '/ajustements', label: 'Ajustements', icon: ArrowLeftRight },
  { href: '/historique', label: 'Historique', icon: Clock },
];

export function BottomNav() {
  const pathname = usePathname();

  return (
    <nav className="fixed bottom-0 left-0 right-0 h-16 bg-background border-t border-border z-50">
      <div className="grid grid-cols-4 h-full">
        {links.map(({ href, label, icon: Icon }) => {
          // Exact match pour /, startsWith pour les sous-routes — pitfall 4 de la RESEARCH
          const isActive = href === '/' ? pathname === '/' : pathname.startsWith(href);
          return (
            <Link
              key={href}
              href={href}
              className={`flex flex-col items-center justify-center gap-1 text-xs min-h-[48px] transition-colors
                ${isActive ? 'text-primary' : 'text-muted-foreground hover:text-foreground'}`}
            >
              <Icon className="h-5 w-5" />
              <span>{label}</span>
            </Link>
          );
        })}
      </div>
    </nav>
  );
}
```

Créer app/(app)/layout.tsx — RSC, pas besoin d'être Client :
```typescript
import { BottomNav } from '@/components/layout/BottomNav';

export default function AppLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="min-h-screen pb-16">
      {children}
      <BottomNav />
    </div>
  );
}
```
Note : pb-16 = padding-bottom 64px = hauteur de la bottom bar. Sans ça, le contenu est masqué par la nav.

Créer app/(app)/page.tsx avec bouton logout (décision CONTEXT.md — en haut à droite, discretion Claude pour placement exact) :
```typescript
import { auth, signOut } from '@/lib/auth/index';
import { Button } from '@/components/ui/button';

export default async function HomePage() {
  const session = await auth();

  return (
    <main className="p-4">
      <div className="flex items-center justify-between mb-6">
        <h1 className="text-xl font-semibold">Accueil</h1>
        <form
          action={async () => {
            'use server';
            await signOut({ redirectTo: '/login' });
          }}
        >
          <Button variant="ghost" size="sm" type="submit">
            Déconnexion
          </Button>
        </form>
      </div>
      <p className="text-muted-foreground">
        Bonjour {session?.user?.id === 'chris' ? 'Chris' : 'Alex'} — Dashboard à venir.
      </p>
    </main>
  );
}
```

Créer les pages placeholder :

app/(app)/depenses/page.tsx :
```typescript
export default function DepensesPage() {
  return (
    <main className="p-4">
      <h1 className="text-xl font-semibold mb-4">Dépenses</h1>
      <p className="text-muted-foreground">À venir.</p>
    </main>
  );
}
```

app/(app)/ajustements/page.tsx :
```typescript
export default function AjustementsPage() {
  return (
    <main className="p-4">
      <h1 className="text-xl font-semibold mb-4">Ajustements</h1>
      <p className="text-muted-foreground">À venir.</p>
    </main>
  );
}
```

app/(app)/historique/page.tsx :
```typescript
export default function HistoriquePage() {
  return (
    <main className="p-4">
      <h1 className="text-xl font-semibold mb-4">Historique</h1>
      <p className="text-muted-foreground">À venir.</p>
    </main>
  );
}
```

Vérifier que app/layout.tsx (root layout) existe — create-next-app l'a normalement créé.
S'il n'existe pas, le créer :
```typescript
import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: 'Le Commun',
  description: 'Comptabilité partagée Chris & Alex',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="fr">
      <body>{children}</body>
    </html>
  );
}
```
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>npm run build passe sans erreur. La bottom nav utilise startsWith pour les sous-routes sauf '/' (exact). pb-16 est présent dans (app)/layout.tsx. Les 4 pages placeholder existent. Le bouton logout est sur la page /.</done>
</task>

</tasks>

<verification>
Après exécution du plan, avec l'app en dev (`npm run dev`) :
1. `npm run build` passe sans erreur TypeScript ni erreur Next.js
2. Visiter http://localhost:3000 redirige vers http://localhost:3000/login (middleware fonctionne)
3. La page /login affiche deux boutons (Chris, Alex) + champ mot de passe
4. Impossible de se connecter sans que les env vars soient configurées (normal — Plan 03 provisionnera Turso et Vercel)
5. Les routes /depenses, /ajustements, /historique redirigent toutes vers /login si non connecté
</verification>

<success_criteria>
- `npm run build` passe sans erreur
- middleware.ts importe depuis lib/auth/index uniquement (vérifiable avec grep)
- La login page a les boutons Chris et Alex (touch targets h-12)
- La bottom nav fixe est présente sur toutes les routes (app)/
- Le bouton déconnexion est sur la page /
- Aucun import de DB dans middleware.ts ni dans les composants Client
</success_criteria>

<output>
After completion, create `.planning/phases/01-fondation/01-02-SUMMARY.md`
</output>
