---
phase: 01-fondation
plan: 03
subsystem: infra
tags: [turso, vercel, playwright, nextauth, drizzle, sqlite, deployment]

# Dependency graph
requires:
  - phase: 01-01
    provides: Drizzle schema, NextAuth config, DB client
  - phase: 01-02
    provides: Login page, BottomNav, protected layout, Playwright test infrastructure
provides:
  - Turso DB provisioned with 3 tables (mois, depenses, ajustements)
  - Vercel production deployment with CD on push to main
  - All 5 environment variables configured on Vercel
  - 6/6 Playwright auth tests passing (AUTH-01, AUTH-02, AUTH-03)
  - Mobile-verified production URL with touch targets >= 48px
affects:
  - 02-depenses
  - 03-balance
  - 04-historique

# Tech tracking
tech-stack:
  added: [turso CLI, drizzle-kit migrate, Vercel CD]
  patterns: [external service provisioning via human-action checkpoints, E2E validation before visual sign-off]

key-files:
  created: [drizzle/migrations/, .env.local (gitignored)]
  modified: [.gitignore]

key-decisions:
  - "Turso DB provisioned by user via CLI (turso db create lecommun) — DB name matches project name"
  - "Vercel CD linked to GitHub main branch — automatic deploy on push"
  - "AUTH_SECRET mandatory for NextAuth v5 — MissingSecret error without it"
  - "Mobile visual sign-off done in Chrome DevTools responsive mode (390x844 iPhone 14)"

patterns-established:
  - "Infrastructure tasks use checkpoint:human-action for external services (Turso, Vercel) then type=auto for validation"
  - "Playwright tests run against localhost with .env.local credentials before production sign-off"

requirements-completed: [INF-01, INF-02, INF-03, AUTH-01, AUTH-02, AUTH-03]

# Metrics
duration: ~3h (including human setup time for Turso + Vercel)
completed: 2026-03-04
---

# Phase 1 Plan 03: Infrastructure & Deployment Summary

**Turso DB live with 3 tables, Vercel production deployment with CD, and 6/6 Playwright auth tests green on mobile-verified app**

## Performance

- **Duration:** ~3h total (including user setup time for Turso CLI + Vercel dashboard)
- **Started:** 2026-03-04T15:00:00Z (approx, after plan 01-02 completed)
- **Completed:** 2026-03-04T16:30:00Z (approx)
- **Tasks:** 4 completed (2 human-action, 1 auto, 1 human-verify)
- **Files modified:** 2 (drizzle migrations generated, .gitignore updated)

## Accomplishments

- Turso DB provisioned with all 3 tables (mois, depenses, ajustements) via drizzle-kit migrate
- Vercel production deployment live at https://lecommun-qao44lcl1-christophe-seguinots-projects.vercel.app with CD on push to main
- All 5 environment variables configured on Vercel (TURSO_DATABASE_URL, TURSO_AUTH_TOKEN, AUTH_SECRET, CHRIS_PASSWORD, ALEX_PASSWORD)
- 6/6 Playwright auth tests passing: chris login, alex login, wrong password, session persists, redirect unauthenticated, redirect authenticated
- Mobile visual verification approved: touch targets >= 48px, font >= 16px, bottom bar navigation, route protection confirmed

## Task Commits

Each task was committed atomically:

1. **Task 1: Turso DB + .env.local** - done by user (no commit — .env.local is gitignored)
2. **Task 2: Vercel deployment** - done by user (external service, no code commit needed)
3. **Task 3: Playwright AUTH tests** - `0cc0a60` (test: Playwright auth tests passent — 6/6 verts)
4. **Task 4: Mobile visual verification** - approved by user (no code changes needed)

**Supporting chore commit:** `5f9028b` (chore: add gitignore, docs, drizzle migrations, remove conflicting root page)

## Files Created/Modified

- `drizzle/migrations/` - SQL migration files generated by drizzle-kit, applied to Turso DB
- `.gitignore` - Updated to exclude .env.local and other sensitive files
- `.env.local` - Created locally (gitignored) with TURSO_DATABASE_URL, TURSO_AUTH_TOKEN, AUTH_SECRET, CHRIS_PASSWORD, ALEX_PASSWORD

## Decisions Made

- Turso DB name `lecommun` matches project — makes CLI commands predictable
- Vercel linked to main branch directly (no staging branch) — simple 2-person project, no need for staging environment
- AUTH_SECRET must be set as Vercel env var before first deploy — NextAuth v5 throws MissingSecret without it
- Mobile check done in Chrome DevTools (390x844) rather than physical device — sufficient for button sizing verification

## Deviations from Plan

None - plan executed exactly as written. All checkpoint:human-action tasks completed by user, type=auto Playwright task passed on first run, checkpoint:human-verify approved without issues.

## Issues Encountered

None — all tasks completed cleanly. The Playwright cookie name for NextAuth v5 was handled correctly in the existing test infrastructure from plan 01-02.

## User Setup Required

**External services required manual configuration (already completed):**

- Turso: `turso db create lecommun` + `npx drizzle-kit migrate` — tables now live
- Vercel: Project imported from GitHub, 5 env vars set, CD active on push to main
- Local: `.env.local` created with all credentials (gitignored, never committed)

All setup is complete. No further manual steps required.

## Next Phase Readiness

- Infrastructure fully operational — DB, auth, deployment all working in production
- Phase 1 (Fondation) is COMPLETE: all 3 plans executed, all 6 requirements satisfied
- Phase 2 (Depenses) can start immediately: DB tables ready to receive data, auth protecting all routes, mobile UI confirmed working
- No blockers or concerns

---
*Phase: 01-fondation*
*Completed: 2026-03-04*
