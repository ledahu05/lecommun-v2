---
phase: 07-modales-et-saisie-rapide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/dialog.tsx
  - components/depenses/DepenseForm.tsx
  - components/ajustements/AjustementForm.tsx
  - app/(app)/depenses/page.tsx
  - app/(app)/ajustements/page.tsx
  - app/(app)/depenses/actions.ts
  - app/(app)/ajustements/actions.ts
autonomous: true
requirements: [MOD-01, MOD-02, MOD-03, MOD-04, UX-01, UX-02]

must_haves:
  truths:
    - "L'utilisateur clique un bouton Ajouter sur /depenses et une modale Dialog s'ouvre avec le formulaire depense"
    - "L'utilisateur clique un bouton Ajouter sur /ajustements et une modale Dialog s'ouvre avec le formulaire ajustement"
    - "Les sous-categories sont affichees en grille de boutons cliquables, pas un select"
    - "Apres soumission reussie, la modale se ferme et les donnees se rafraichissent"
    - "En cas d'erreur de validation, la modale reste ouverte avec un message d'erreur visible"
    - "Aucun formulaire inline Card n'est visible sur les pages liste"
  artifacts:
    - path: "components/ui/dialog.tsx"
      provides: "shadcn Dialog primitive"
    - path: "components/depenses/DepenseForm.tsx"
      provides: "Formulaire depense en modale Dialog"
      contains: "DialogContent"
    - path: "components/ajustements/AjustementForm.tsx"
      provides: "Formulaire ajustement en modale Dialog"
      contains: "DialogContent"
    - path: "app/(app)/depenses/page.tsx"
      provides: "Page liste depenses sans formulaire inline"
    - path: "app/(app)/ajustements/page.tsx"
      provides: "Page liste ajustements sans formulaire inline"
  key_links:
    - from: "components/depenses/DepenseForm.tsx"
      to: "app/(app)/depenses/actions.ts"
      via: "server action call with error return handling"
      pattern: "actionCreateDepense"
    - from: "components/ajustements/AjustementForm.tsx"
      to: "app/(app)/ajustements/actions.ts"
      via: "server action call with error return handling"
      pattern: "actionCreateAjustement"
    - from: "components/depenses/DepenseForm.tsx"
      to: "components/ui/dialog.tsx"
      via: "Dialog/DialogTrigger/DialogContent wrapper"
      pattern: "Dialog.*DialogTrigger.*DialogContent"
---

<objective>
Refactorer les formulaires depense et ajustement de Card inline vers des modales Dialog shadcn/ui, avec sous-categories en grille de boutons.

Purpose: Les formulaires inline prennent trop de place sur les pages liste. Les modales permettent de garder la liste visible et de saisir rapidement. Les sous-categories en boutons sont plus rapides que le select natif sur mobile.
Output: Deux composants modaux (DepenseForm, AjustementForm) utilisant Dialog shadcn/ui, integres dans les pages /depenses et /ajustements.
</objective>

<execution_context>
@/home/ledahu/.claude/get-shit-done/workflows/execute-plan.md
@/home/ledahu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@components/depenses/DepenseForm.tsx
@components/ajustements/AjustementForm.tsx
@app/(app)/depenses/page.tsx
@app/(app)/ajustements/page.tsx
@app/(app)/depenses/actions.ts
@app/(app)/ajustements/actions.ts
@lib/categories.ts

<interfaces>
<!-- Server actions return type -- both return { error: string } | void -->

From app/(app)/depenses/actions.ts:
```typescript
export async function actionCreateDepense(formData: FormData): Promise<{ error: string } | void>
export async function actionDeleteDepense(formData: FormData): Promise<void>
```

From app/(app)/ajustements/actions.ts:
```typescript
export async function actionCreateAjustement(formData: FormData): Promise<{ error: string } | void>
export async function actionDeleteAjustement(formData: FormData): Promise<void>
```

From lib/categories.ts:
```typescript
export const CATEGORIES = { alimentation: {...}, habitation: {...}, loisirs: {...}, vie_quotidienne: {...} } as const;
export type Categorie = keyof typeof CATEGORIES;
export type PayePar = 'chris' | 'alex';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installer Dialog shadcn et refactorer DepenseForm en modale avec sous-categories boutons</name>
  <files>components/ui/dialog.tsx, components/depenses/DepenseForm.tsx, app/(app)/depenses/page.tsx, app/(app)/depenses/actions.ts</files>
  <action>
1. Installer le composant Dialog shadcn/ui: `npx shadcn@latest add dialog`

2. Refactorer `components/depenses/DepenseForm.tsx` en composant modal:
   - Wrapper Dialog + DialogTrigger (bouton "Ajouter une depense" avec icone Plus, min-h-[48px]) + DialogContent + DialogHeader + DialogTitle
   - Gerer l'etat `open` avec useState pour pouvoir fermer la modale programmatiquement
   - Remplacer le `<select>` des sous-categories par une grille de boutons (`grid grid-cols-3 gap-2`), chaque bouton avec `min-h-[44px]` et variant default/outline selon selection. Utiliser un state `sousCategorie` initialement la premiere sous-cat de la categorie selectionnee. Quand la categorie change, reset sousCategorie sur la premiere sous-cat de la nouvelle categorie.
   - Conserver l'input hidden `name="sous_categorie"` avec la valeur du state
   - Pour la gestion d'erreur: ne plus utiliser `form action={...}`. Utiliser un `onSubmit` handler qui appelle `actionCreateDepense` via une fonction async, capture le retour `{ error }`, et si erreur, setError(msg) + ne pas fermer. Si succes, setOpen(false) pour fermer la modale. Utiliser `useTransition` de React pour le pending state.
   - Afficher le message d'erreur dans un `<p className="text-sm text-destructive">` au-dessus du bouton submit, avec data-testid="depense-error"
   - Apres fermeture reussie, appeler `router.refresh()` via `useRouter` de next/navigation pour rafraichir les donnees RSC
   - Conserver tous les data-testid existants (data-testid="depense-form")
   - Conserver min-h-[48px] et text-base sur tous les champs (mobile-first)
   - Le DialogContent doit avoir `className="max-h-[90vh] overflow-y-auto"` pour le scroll sur petits ecrans

3. Modifier `app/(app)/depenses/actions.ts`:
   - actionCreateDepense doit aussi `revalidatePath('/ajustements')` en plus de `/depenses` et `/` (coherence cross-page)

4. Modifier `app/(app)/depenses/page.tsx`:
   - Supprimer l'import et le rendu de `<DepenseForm />` en tant que Card inline
   - Ajouter `<DepenseForm />` qui est maintenant le composant modale (le trigger est DANS le composant)
   - Placer le bouton trigger au-dessus de la liste: `<div className="flex justify-between items-center mb-4"><h1>...</h1><DepenseForm /></div>`
  </action>
  <verify>
    <automated>cd /home/ledahu/workspace/lecommun && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>La page /depenses affiche un bouton "Ajouter" qui ouvre une modale Dialog contenant le formulaire. Les sous-categories sont des boutons en grille. Le formulaire inline Card a disparu. La modale se ferme apres soumission reussie. Les erreurs s'affichent inline dans la modale.</done>
</task>

<task type="auto">
  <name>Task 2: Refactorer AjustementForm en modale Dialog</name>
  <files>components/ajustements/AjustementForm.tsx, app/(app)/ajustements/page.tsx, app/(app)/ajustements/actions.ts</files>
  <action>
1. Refactorer `components/ajustements/AjustementForm.tsx` en composant modal (meme pattern que DepenseForm):
   - Wrapper Dialog + DialogTrigger (bouton "Ajouter un ajustement" avec icone Plus, min-h-[48px]) + DialogContent + DialogHeader + DialogTitle
   - Gerer l'etat `open` avec useState
   - Meme pattern onSubmit handler avec useTransition: appeler `actionCreateAjustement`, capturer le retour `{ error }`, si erreur setError, si succes setOpen(false) + router.refresh()
   - Afficher le message d'erreur dans `<p className="text-sm text-destructive" data-testid="ajustement-error">`
   - Conserver le pattern segmented buttons pour de/vers (deja en place)
   - Conserver tous les data-testid existants (data-testid="ajustement-form")
   - Conserver min-h-[48px] et text-base sur tous les champs
   - DialogContent avec `className="max-h-[90vh] overflow-y-auto"`

2. Modifier `app/(app)/ajustements/actions.ts`:
   - actionCreateAjustement doit aussi `revalidatePath('/depenses')` en plus de `/ajustements` et `/`

3. Modifier `app/(app)/ajustements/page.tsx`:
   - Meme refactoring que depenses/page.tsx: supprimer le formulaire inline, placer le composant AjustementForm (qui contient son propre trigger) a cote du titre h1
  </action>
  <verify>
    <automated>cd /home/ledahu/workspace/lecommun && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>La page /ajustements affiche un bouton "Ajouter" qui ouvre une modale Dialog contenant le formulaire. Le formulaire inline Card a disparu. La modale se ferme apres soumission reussie. Les erreurs s'affichent inline dans la modale.</done>
</task>

<task type="auto">
  <name>Task 3: Verifier les tests E2E existants et corriger si necessaire</name>
  <files></files>
  <action>
Les tests E2E existants (tests/depenses.spec.ts, tests/ajustements.spec.ts) utilisent des selecteurs comme `data-testid="depense-form"` et `data-testid="ajustement-form"`. Ces selecteurs sont preserves dans les modales.

Cependant, les tests doivent peut-etre cliquer sur le bouton trigger de la modale avant de remplir le formulaire.

1. Lancer `npx playwright test tests/depenses.spec.ts tests/ajustements.spec.ts --reporter=line` pour voir si les tests passent encore
2. Si des tests echouent parce que le formulaire n'est plus visible immediatement:
   - Ajouter un clic sur le bouton trigger de la modale (le bouton contenant "Ajouter") AVANT de remplir le formulaire
   - Le pattern sera: `await page.getByRole('button', { name: /ajouter/i }).click()` puis attendre que le dialog soit visible: `await page.getByRole('dialog').waitFor()`
3. Re-lancer les tests pour confirmer qu'ils passent
4. Lancer la suite complete: `npx playwright test --reporter=line` pour verifier aucune regression
  </action>
  <verify>
    <automated>cd /home/ledahu/workspace/lecommun && npx playwright test --reporter=line 2>&1 | tail -30</automated>
  </verify>
  <done>Tous les tests E2E existants passent (depenses, ajustements, balance, historique). Aucune regression.</done>
</task>

</tasks>

<verification>
- `npm run build` passe sans erreur
- `npx playwright test --reporter=line` -- tous les tests existants passent
- Sur /depenses: aucun formulaire Card inline visible, bouton "Ajouter" ouvre une modale avec sous-categories en boutons
- Sur /ajustements: aucun formulaire Card inline visible, bouton "Ajouter" ouvre une modale
- Soumission reussie ferme la modale et rafraichit la liste
- Erreur de validation garde la modale ouverte avec message d'erreur visible
</verification>

<success_criteria>
- Les formulaires depense et ajustement sont dans des modales Dialog (plus de Card inline)
- Les sous-categories sont selectionnees via boutons en grille (plus de select)
- La modale se ferme automatiquement apres soumission reussie
- Les erreurs de validation s'affichent inline dans la modale
- Tous les tests E2E existants passent sans regression
</success_criteria>

<output>
After completion, create `.planning/phases/07-modales-et-saisie-rapide/07-01-SUMMARY.md`
</output>
