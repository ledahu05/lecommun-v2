---
phase: 03-saisie
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified: []
autonomous: false
requirements: [DEP-01, DEP-02, DEP-03, DEP-04, DEP-05, AJU-01, AJU-02, AJU-03, AJU-04]

must_haves:
  truths:
    - "npx playwright test runs the full suite with 0 failures"
    - "All 9 Phase 3 requirement tests are green (DEP-01..DEP-05, AJU-01..AJU-04)"
    - "The existing Phase 2 tests (DASH-01..DASH-04, RPT-01) are still green"
    - "The user has manually confirmed the mobile UX on a 390px viewport"
  artifacts:
    - path: "tests/depenses.spec.ts"
      provides: "5 green tests"
    - path: "tests/ajustements.spec.ts"
      provides: "4 green tests"
  key_links:
    - from: "Full Playwright suite"
      to: "Phase 3 requirements"
      via: "npx playwright test"
      pattern: "passed.*9"
---

<objective>
Gate task: run the full Playwright suite to confirm all Phase 3 tests are green and no regression in Phase 2. Then present the working mobile UI to the user for sign-off.

Purpose: Phase 3 is only complete when all tests pass AND the mobile UX is confirmed by a human. The app replaces Google Sheets — both users need to trust it.
Output: Green CI, human approval of the daily data-entry flow.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-saisie/03-02-SUMMARY.md
@.planning/phases/03-saisie/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full Playwright suite — confirm all tests green</name>
  <files></files>
  <action>
Run the full Playwright test suite. If any test fails, diagnose the failure and fix it. Do not skip or mark tests fixme.

Steps:
1. Run `npx playwright test` from /home/chris/workspace/lecommun
2. If failures occur: read the error, identify the root cause (selector mismatch, revalidatePath missing, Zod coerce issue, etc.), fix the component/action/test file that is wrong
3. Re-run until 0 failures
4. Confirm that the Phase 2 balance tests (DASH-01..DASH-04, RPT-01) are still green — their data-testid attributes must not have been disturbed

If the shadcn select/input needed for sous_categorie is not behaving correctly in Playwright (e.g., shadcn Select uses a Radix portal that Playwright can't click), replace the shadcn Select with a native `<select>` element in DepenseForm — native select works reliably with Playwright `page.selectOption()`.
  </action>
  <verify>
    <automated>cd /home/chris/workspace/lecommun && npx playwright test 2>&1 | tail -15</automated>
  </verify>
  <done>Full suite: 0 failed. All tests from Phase 2 (auth, balance) and Phase 3 (depenses, ajustements) pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 3 feature:
    - /depenses page: form with catégorie/sous-catégorie cascade, segmented Chris/Alex buttons, montant, date (default today), libellé optionnel. List of current month expenses with delete button. Montant ≤ 0 rejected.
    - /ajustements page: form with direction (Chris→Alex or Alex→Chris via segmented buttons), montant, libellé obligatoire, date. List with delete. Adding/deleting an ajustement recalculates the dashboard balance.
    - All 9 Phase 3 E2E tests green.
  </what-built>
  <how-to-verify>
    Open http://localhost:3000 in a browser at 390px width (DevTools → iPhone 14 viewport).

    1. Go to /depenses. Verify form is usable with touch-size buttons (min 48px). Select catégorie "Habitation", verify sous-catégories update to: loyer, energie, internet, eau, assurance, amazon, netflix. Select "Chris" as payeur. Enter montant "115". Enter label "Appart". Submit. Verify the new expense appears in the list below.

    2. Test validation: enter montant "0". Submit. Verify NO new item appears in the list (error expected — could be HTML5 validation preventing submit, or server returns error).

    3. Delete the expense you just created. Verify the list shows "Aucune dépense ce mois-ci."

    4. Go to /ajustements. Verify form is visible. Select "Alex" (de=Alex, vers=Chris is implicit). Enter montant "50". Enter label "Remboursement Alex". Submit. Verify it appears in the list.

    5. Go to / (dashboard). Verify the balance has changed to reflect the ajustement (balance_finale now shows Alex→Chris adjustment).

    6. Go back to /ajustements. Delete the ajustement. Go to /. Verify balance returns to previous value (or zero if no other data).

    7. Confirm bottom navigation works: /, /depenses, /ajustements links all navigate correctly, no broken pages.
  </how-to-verify>
  <resume-signal>Type "approved" if the flow works correctly on mobile viewport, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 3 is complete when:
- npx playwright test shows 0 failures across all spec files
- Human has typed "approved" after manual mobile verification
</verification>

<success_criteria>
- Full Playwright suite green (Phase 2 + Phase 3 tests: 0 failures)
- User confirms mobile UX at 390px viewport: forms usable, touch targets adequate, balance recalculates after mutations
- Phase 3 requirements DEP-01..DEP-05 and AJU-01..AJU-04 all satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-saisie/03-04-SUMMARY.md`
</output>
